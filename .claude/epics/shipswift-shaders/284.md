---
name: 收尾——署名 / probe / 文档索引 / 预览宿主 / 性能基准
status: closed
created: 2026-09-04T07:19:16Z
updated: 2026-09-25T01:07:54Z
github: https://github.com/wxlpp/CoreDesign/issues/284
depends_on: [256, 279, 281, 282, 283] # 256 = shipswift-effects A-7（open，仍阻塞本 task）
parallel: false
conflicts_with: []
---

# Task: 收尾 —— 署名 / probe / 文档索引 / 预览宿主 / 性能基准（B-4）

⚠️ 本 task 的交付物已按 **α 改判**与**实际落地量**重写：草案里与 β 绑定的项（metallib
体积核验等）作废；shader 总数从 28 降到 **8（已落地）+ 最多 11（B-2 / B-3）**。

## 署名

- [ ] `ACKNOWLEDGEMENTS.md` **追加**逐 shader 条目（骨架由 #249 建，本 task 不新建）
- [ ] **Apache-2.0 段**（paper-design/shaders）：LICENSE 全文 + `NOTICE`
      （`Powered by Paper Shaders: https://shaders.paper.design`）+ 修改标注（§4(b)）
- [ ] **无来源不明项被落地** —— 与《统一裁定表》逐行对齐
- [ ] ⚠️ **`epic → main` 前的硬闸**：`docs/shader-provenance.md` 里**零 `TBD`**、
      强指纹项全部追完（见 #281）。**该闸不在本 task 射程内，但本 task
      是它的最后一道核对点**

## probe 实质调用点

- [ ] **probe 覆盖本 epic 全部已落地 shader 类型**的 nonisolated 调用点
      （#260 只接了 Effects / Charts 两个 product，Shaders 未接；#247 只做接线）
      ⚠️ 措辞刻意不写「全部**公开**值类型」——那是自指的
- [ ] **分流规则**见 `EffectsNonisolatedUsage.swift` 文件头：**值类型 / 配置类型 / 数据入参**
      进 `*NonisolatedUsage.swift`；**View / modifier / style 类**进 `PublicVisibility.swift`
      （`@MainActor`）——本包开了 `.defaultIsolation(MainActor.self)`，View 的 `init` 与
      modifier 函数天然是 MainActor 隔离的，从 `nonisolated func` 里构造必然编译失败
- [ ] `cd scripts/downstream-probe && swift build` 绿
- [ ] ⚠️ **α 的残余风险须在 probe 里体现**：probe 用**原生 `swift build`**，而 α 下
      原生构建不产 metallib ⇒ probe **只能 build-only、不得触发渲染**，否则会打到
      「静默无渲染」那条路上。此处须留一条注释说明为什么不在 probe 里跑渲染

## 文档与索引

- [ ] 每个已落地 shader 有 `docs/components/*.md`
- [ ] 每份组件文档带 **α 残余风险的同一句话**：「用原生 `swift build` 消费本 product 时
      须加 `--build-system swiftbuild`」（#248 要求三处同文：README / CLAUDE.md / 逐组件文档）
- [ ] **`docs/README.md` 索引**已挂，落点按 #244 的 AD-4 裁决
      ⚠️ AD-4 已收敛为「**AD-2 原样适用**」——三个 target 全走 `component-registry.json`，
      没有「a vs b」这个待决问题 ⇒ 草案里那段「判据随 AD-4 走向而不同」**已失效**，
      判据就是 #279 交付的差集守卫
- [ ] `docs/reachable-type-registry.json` 已登记本 epic 新增的可达类型
      ⚠️ **不写「`ReachableTypeRegistryGuard` 绿」**——它只查 schema / 与登记表不相交 /
      参数名唯一 / 全 C 四条，**不扫源码**，对新 target 是空真

## 预览宿主与快照

- [ ] `App/Sources/ComponentData.swift` 补本 epic 的画廊条目
      ⚠️ **必须在 `shipswift-effects` #256 定义的串行写入窗口之外写**——这是剩下的唯一
      结构性跨 epic 耦合
- [ ] `scripts/run-preview.sh` 能构建并展示全部已落地 shader
- [ ] 快照排除策略：shader 是逐帧变化的，产出**非确定 PNG** ⇒ 须进 #256 定的排除策略
      （不另立一套）

## 性能基准

- [ ] 复用 #256 建的 NFR-1 性能基准脚本（**不重造**）跑本 epic 的 `colorEffect`
- [ ] **脚本断言而非人工抽测**，且**在真机执行**——Simulator 上的 GPU frame-time 无意义
- [ ] ⚠️ 若 #282 先于 #256 完成，harness 应已在 `shipswift-effects` #252 落地（该 task
      的可选 AC），本 task 复用它

## Epic 级验收（按 α 与实际落地量重判）

- [ ] 可落地件 **100%「可用」**（四条 AND：编译 / 过守卫 / 有 Preview 且进画廊 / 有文档）
- [ ] **`CoreDesignEffects` 的反向依赖禁令机器化**：
      `swift package describe --type json | jq '.targets[] | select(.name=="CoreDesignEffects") | .target_dependencies'`
      恰为 `["CoreDesign"]`（Effects 不得依赖 Shaders）
- [ ] **NFR-5② 沿用**：`CoreDesignTests` 的 `target_dependencies` 仍恰为 `["CoreDesign"]`
- [ ] **`CoreDesignShaders` 的 resource bundle 每平台 ≤ 2MB**
      ⚠️ **口径按 α 改**：仓库里**没有**提交任何 metallib，被测对象是**构建产物**；
      「β 下 3 份合计 ≤ 6MB」那半句作废。#248 实测边际 ≈ 9.2 KB/shader ⇒ 19 个 ≈ 175 KB，
      远低于阈值 ⇒ **本条现在是形式性核对，不是风险项**
- [ ] **Bool 豁免基线净增 ≤ 1 条**（AD-H），有书面理由，按棘轮脚本抬基线
- [ ] CI 四条腿全绿

## ⚠️ 草案里已作废的验收项（照录，勿再执行）

- ~~metallib sha256 同步守卫带触发红的 fixture~~ —— β 独有，α 下 `.metal` 与产物同一次
  构建，不可能失步
- ~~NFR-3 明写不支持 Mac Catalyst~~ —— β 独有（`#if os(macOS)` 在 Catalyst 下为假会误选
  `iphoneos` 份）；α 下构建系统自己处理

## Dependencies

- 本 epic 的 #282 / #283 / #279 / #281 全部完成
- **`shipswift-effects` #256（A-7）** —— `ComponentData.swift` 串行窗口 + NFR-1 性能基准脚本。
  ⚠️ #256 现为 **open**，且它自己依赖 #250~#255（#251 已拆成 #266 / #267 / #268，均 open）
  ⇒ **B-4 仍被阻塞**，这是本 epic 与 Epic A 之间唯一剩下的结构性耦合

## Effort Estimate
- Size: L
- Hours: 8–14（草案按 28 个估；实际 ≤ 19 个，且 α 省掉了 metallib 一整块）

## Definition of Done
- [ ] Code implemented
- [ ] CI 四条腿全绿
- [ ] 性能基准在真机跑过并留证
- [ ] Code reviewed
