---
name: 3 个可落地 layerEffect 内容层效果
status: closed
created: 2026-09-04T07:19:16Z
updated: 2026-09-05T04:30:00Z
github: https://github.com/wxlpp/CoreDesign/issues/283
depends_on: [279, 280]
parallel: true
conflicts_with: [282] # 同写 Sources/CoreDesignShaders/CoreDesignShaders.metal 与 docs/component-registry.json
---

# Task: 3 个可落地 `layerEffect` 内容层效果（B-3，按裁定表重列）

## ⚠️ 成员按《统一裁定表》重列 —— 草案的「11 个」不成立

epic 草案写 B-3 = **11 个** `layerEffect`（那是 28 个全落地时的分类数）。
闸②裁定可落地 11 **总数**，其中属 `layerEffect` 的只有 **3 个**：

| # | shader | 裁定 | 上游 |
|---|---|---|---|
| 1 | `GlassOrb` | 已追到兼容许可 · **MIT** | Inferno 的 "Warping Loupe"（Paul Hudson） |
| 2 | `Water` | 已追到兼容许可 · **Apache-2.0** | paper `water.ts` |
| 3 | `Halftone` | 同上 | paper `halftone-dots.ts` + `halftone-cmyk.ts`（**ShipSwift 自己在 `SWHalftone.metal:350` 写着 "simplified port"**） |

## ⚠️ AD-F 的「layerEffect 段难度显著高于 colorEffect」**对本批不成立**

AD-F 的依据是「7 个颜色写死件全是 `layerEffect`」——那 7 个是
`ChromaticGlass` / `Foil` / `Glitter` / `IntenseBling` / `PolishedAluminum`（ShaderKit 5）
+ `GlassLogo` + `LiquidMetal`。**它们没有一个在可落地 11 里**（ShaderKit 5 与
`LiquidMetal` 判 `待追溯`；`GlassLogo` 判 `待追溯` 且已由 #261 以 `GlassSymbol` 之名落地）。
⇒ 本批 3 件**不含**任何「把 `.metal` 里的调色板全部提到 Swift 侧」的改造，
#248 给的 **2–3 小时/个**难件工时**不适用**，按 **1–1.5 小时/个**。

⚠️ 连带：**#248 交付 B 里那 7 个难件的工时估算彻底失去用武之地**，
而 `N_B` = 5 的分母（2.0–2.5h/shader）正是靠「可能落地的集合里有 7 个难件」加权出来的
⇒ **分母偏高** ⇒ `N_B` 偏大 ⇒ 闸②的门槛被高估。#280 的 `N_B` 重估须并入这一条。

## ⚠️ 一处须在开工时确认的分类

`Water` 在 epic 的分类表里属 `layerEffect`（该分类来自 ShipSwift 快照的实现形态），
而它的上游 paper `water.ts` 是**背景**类效果。两者不必然矛盾（ShipSwift 可以把它做成
作用于内容层的形态），但**开工第一件事是读 `SWWater.metal` 的签名确认它到底是
`colorEffect` 还是 `layerEffect`**；若实为 `colorEffect`，成员移到 #282、本 task 剩 2 件。

## Acceptance Criteria

- [ ] 3 个落成 `public struct … : ViewModifier` + `public extension View` 便利方法（AD-E）
- [ ] `.metal` 侧零硬编码色；tint 以参数传入（沿用 #261 的形态——shader 读不到 `.tint`）
- [ ] **必须 `#include <SwiftUI/SwiftUI_Metal.h>`**，否则
      `error: use of undeclared identifier 'SwiftUI'`（#248 ⑤ 实测）
- [ ] **Reduce Motion**：冻结**时间**输入，但**保留手势/倾斜的空间输入**
      （放大镜跟手是交互不是动效）——`GlassOrb` 是这条的主要对象
- [ ] **Reduce Transparency**：`GlassOrb` 降级为不透明形态
      （⚠️ SC 原文点名 `Glass` / `GlassOrb` / `ChromaticGlass` 三个，但 `Glass` 已由 #261
      以 `RefractiveGlass` 落地、`ChromaticGlass` 判 `待追溯` ⇒ 本 task 只承接 `GlassOrb`；
      `RefractiveGlass` 那一条归 B-4 复核）
- [ ] **`GlassOrb` 的触控目标测试**在 `CoreDesignShadersTests` 内**同形态实现**
      ——⚠️ **不得**加进 `TouchTargetTests`，那会判红 NFR-5②
      （`CoreDesignTests` 的 `target_dependencies` 须恰为 `["CoreDesign"]`）
- [ ] 像素采样的 render proof（沿用 #261 的 `RenderProofTests` 形态）
- [ ] `Water` / `Halftone` 的 Apache-2.0 署名义务四条（LICENSE + `NOTICE` + 修改标注 +
      `.metal` 文件头注明对应 `.ts` 路径）
- [ ] 各有 `#Preview`；`EffectsColorLiteralGuard` 对本批零违规
- [ ] **FR-13**：shader 装饰层 100% `accessibilityHidden(true)`

## Dependencies

- **#280（落地前核验闸）**（`GlassOrb` 的「推论为 Inferno 原创」须复核；
  `Water` / `Halftone` 的「paper 之上还有一层」未查）—— **硬前置**
- **#279（扫描根收口）**
- ⚠️ **不依赖 #271 / #252** —— `layerEffect` 是内容层效果、没有常驻时间驱动的能耗判定面，
  NFR-7 的注入键不进本批（若开工时发现 `Water` 有常驻动画，按 B-2 同款处置并补依赖）

## Effort Estimate
- Size: S
- Hours: 4–7

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
