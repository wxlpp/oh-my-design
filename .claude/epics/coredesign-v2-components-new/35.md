---
name: SidebarRow
status: closed
created: 2026-05-09T22:32:29Z
updated: 2026-05-10
github: https://github.com/wxlpp/CoreDesign/issues/35
depends_on: []
parallel: true
conflicts_with: []
---

# Task: SidebarRow

## Description

新增 GitHub 桌面客户端风格侧栏行 `SidebarRow`：选中态左侧 2pt accent 条 + 选中 / hover 态浅色背景 + 紧凑高度。

## Acceptance Criteria

### API 形态
- [ ] 新建 `Sources/CoreDesign/Components/SidebarRow/SidebarRow.swift`
- [ ] `public struct SidebarRow<Label: View>: View`
- [ ] `public init(isSelected: Bool, @ViewBuilder label: () -> Label)`
- [ ] `SidebarRow(_ text: String, isSelected: Bool)` convenience init `where Label == Text`

### 视觉
- [ ] **selected 态**：左侧 `CoreBorderWidth.thick` (2pt) accent 条 + `Color.borderFocus` 颜色（**用 token 引用，不是字面量 `2`**）+ 背景 `Color.surfaceCanvasSubtle`
- [ ] **hover 态**（非 selected）：背景 `Color.surfaceCanvasSubtle`（用 `@State private var isHovered = false` + `.onHover(perform:)` 跟踪）
- [ ] **默认态**（非 selected 非 hover）：透明背景
- [ ] label 字号：`CoreTypography.bodyMediumFont`，foregroundStyle `Color.contentPrimary`
- [ ] 高度：`frame(minHeight: CoreControlMetrics.height(for: .small))`（紧凑；per PRD FR-B-5）
- [ ] padding：`CoreControlMetrics.horizontalPadding(for: .small)` + `CoreControlMetrics.verticalPadding(for: .small)`

### Hover token debt（**doc-comment 用同一段固定文字**，与 ListRow 保持一致）
- [ ] doc-comment 内固定段落：
  > "hover 态使用 `Color.surfaceCanvasSubtle` 而非 `Color.hoverBackground`：后者已存在于 `InteractionColors.swift` 但取值是系统 fill 未对齐 Primer。本组件直接用 `surfaceCanvasSubtle` 是**取值层取舍**，不是 token 缺失代偿。详见 PRD `coredesign-v2-components.md` §Notes hover token debt。"

### Token-clean
- [ ] `SidebarRow.swift` production 代码零 magic-numbers
- [ ] **特别**：accent 条厚度必须用 `CoreBorderWidth.thick` token（per epic ADR #7 / PRD FR-B-5），不得 inline `2`

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 + selected / hover / default 三态切换视觉合理

### PR description 5 字段
- [ ] 5 字段全部填写
- [ ] doc-comment 完整性勾选（含 hover token debt 段，与 ListRow 同款措辞）

## Technical Details

参考 epic.md `### 输入 / 列表 / 侧栏`。本组件在视觉构成上有一处特别约束：accent 条不是 `Rectangle` 字面 2pt 宽度，而是 `Rectangle().frame(width: CoreBorderWidth.thick)`——避免直接 inline。

如视觉发现 2pt accent 条偏窄（譬如 GitHub 桌面客户端实际是 3-4pt），可改用 `CoreBorderWidth.thicker`（4pt）；记入 PR description。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 6（0.75 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
