---
name: Toast（architecture spike）
status: closed
created: 2026-05-09T22:32:29Z
updated: 2026-05-10
github: https://github.com/wxlpp/CoreDesign/issues/36
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Toast（architecture spike）

## Description

新增 scene-scoped 全局通知组件 `Toast`。架构最复杂，是 epic 内唯一的 spike——`@MainActor @Observable final class ToastHost` + `EnvironmentValues` 注入 + `safeAreaInset` 渲染 + 队列状态机 + 手势 / 自动 / 点击 dismiss。

**不接受 singleton 妥协交付**（per epic ADR #9 + PRD R2 修订）：如 `@MainActor @Observable` + Environment 注入路径阻塞且 0.5d spike 内无解，本 task 应**作为 blocker 标记 + 升级给用户介入**，不降级到 singleton。

**本 task 内不再拆**（per epic ADR #14）：`ToastHost` / `ToastItem` / `ToastLevel` / `EnvironmentValues.toastHost` / `View.toastHost(edge:)` / 内部 `ToastView` 全部属同一 task 的实现细节。

## Acceptance Criteria

### API 形态（per PRD FR-B-7）
- [ ] 新建 `Sources/CoreDesign/Components/Toast/Toast.swift`（含全部 API + 内部 ToastView）
- [ ] `@MainActor @Observable public final class ToastHost`：
  - `public init()`
  - `public func show(_ message: String, level: ToastLevel = .info, duration: TimeInterval = 3)`
  - `public func show(_ item: ToastItem)`
  - `public func dismiss(_ id: ToastItem.ID)`
- [ ] `public struct ToastItem: Identifiable, Sendable`：含 `id: UUID` / `message: String` / `level: ToastLevel` / `duration: TimeInterval`；含 `public init(message:level:duration:)`
- [ ] `public enum ToastLevel: Sendable { case info, success, warning, danger }`
- [ ] `extension EnvironmentValues { @Entry public var toastHost: ToastHost? = nil }`
- [ ] `public extension View { func toastHost(edge: VerticalEdge = .top) -> some View }`

### 队列状态机（per epic ADR #16，硬 AC）
- [ ] **dismiss timing**：`duration` 计时**从 toast 开始显示**起算（**不是** enqueue）；自动 dismiss 用 `Task.sleep(...) + cancel` 模型，task cancellation 必须正确清理
- [ ] **append 状态机**：当前 toast 正在 dismiss 动画中时新 `show(...)` 进队列尾，**不打断**、**不 replace**；正在显示中时 `show(...)` 同样 append；空队列时 `show(...)` 立即开始显示
- [ ] **dismiss 行为**：`dismiss(id)` 对**排队中**的 item 直接从队列移除；对**正在显示**的 item 立即触发 dismiss 动画（dismiss 完成后渲染下一条）
- [ ] **重复触发不 double-fire**：连续 `dismiss(id)` 不引发崩溃 / 重复动画

### z-order 显式限制（per epic ADR #16）
- [ ] `safeAreaInset(edge:)` 仅在挂 `toastHost(edge:)` 那层 view 树内可见，**不**覆盖 sheet / fullScreenCover / 独立 window
- [ ] 这条作为 **PR description 必填字段**（per epic SC#5 Toast 专项 z-order limitation 段）；doc-comment 内同样显式标注，调用方需在每个 sheet root 单独挂 `.toastHost(...)` 才能让 sheet 内触发的 toast 显示

### 视觉
- [ ] 容器：`View.surface(.card)` + `View.coreShadow(.medium)`
- [ ] 字号：`CoreTypography.bodyMediumFont`
- [ ] 按 `ToastLevel` 配 icon：`info.circle` / `checkmark.circle` / `exclamationmark.triangle` / `exclamationmark.octagon`
- [ ] 按 `ToastLevel` 配前景色：`Color.infoForeground` / `successForeground` / `warningForeground` / `dangerForeground`
- [ ] padding：`CoreSpacing.md` 内边距 + `CoreSpacing.lg` 外缘 inset

### Dismiss 触发
- [ ] 自动消失（按 `duration`）
- [ ] 滑动手势消除（向 `edge` 方向滑动）—— SwiftUI `DragGesture`
- [ ] 点击 toast 自身立即 dismiss（不带动画延迟）

### 跨平台
- [ ] iOS 与 macOS 双平台都正常工作（`safeAreaInset` 在两端语义略有差异，需视觉抽查）

### Token-clean
- [ ] `Toast.swift` production 代码零 magic-numbers

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过 0 warning
- [ ] `swift test` 退出 0
- [ ] `#Preview` 演示完整调用链：scene root 挂 `.toastHost()` + 子 view `@Environment(\.toastHost)` + 触发各 ToastLevel
- [ ] `#Preview` light / dark 双模式 4 种 ToastLevel 视觉合理

### PR description 5 字段 + Toast 专项（per epic SC#5）
- [ ] Light preview / Dark preview / API 摘要 / doc-comment 完整性 / token-clean 自检
- [ ] **z-order limitation 段**（Toast 专项）：明示"`safeAreaInset` 仅在 scene root 层可见，不覆盖 sheet / fullScreenCover / 独立 window"；调用方在 sheet 内需重挂 `.toastHost(...)`

## Technical Details

参考 PRD §FR-B-7 + epic.md `### 全局通知（Toast）`。

**Spike 内部节奏建议**：0.5d architecture spike（验证 `@MainActor @Observable` + EnvironmentValues + 单条 toast 渲染）+ 1.5–2.5d 实现（队列状态机 + dismiss 动画 + 手势 + 双平台 #Preview）。

**关键技术点**：
- `@MainActor @Observable` final class 在 Swift 6 strict concurrency 下：`@Observable` 不会自动赋 Sendable；本 task 不需要 `Sendable` 显式约束（`@MainActor` 已隐式提供线程安全）
- `Task.sleep` cancellation：内部维护一个 `dismissTask: Task<Void, Never>?`，每次显示新 toast 前 `dismissTask?.cancel()` 清理上一个
- `@Entry public var toastHost: ToastHost?` 默认值 `nil`：scene 没挂 `toastHost(edge:)` modifier 时 `@Environment(\.toastHost)` 拿到 `nil`，调 `nil?.show(...)` 是 no-op（建议 + 一行 `assertionFailure` debug warning）

## Dependencies

- v2-tokens 全部 merged（含 `View.surface(.card)` + `View.coreShadow(.medium)`；已满足）

## Effort Estimate
- Size: XL
- Hours: 16 - 24（2 - 3 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 + Toast 专项 z-order limitation 段
- [ ] PR review + merge 到 main
- [ ] 若 spike 路径阻塞：blocker 标记 + 升级给用户介入，**不**降级 singleton
