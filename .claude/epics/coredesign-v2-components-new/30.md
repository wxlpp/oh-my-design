---
name: Badge（canary）
status: closed
created: 2026-05-09T22:32:29Z
updated: 2026-05-10
github: https://github.com/wxlpp/CoreDesign/issues/30
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Badge（canary）

## Description

新增 GitHub 风格状态指示器 `Badge`。本 task 是 epic canary——最简单的新组件，验证 `BadgeVariant` 枚举参数化模式 + `surfaceCanvasSubtle` / `borderMuted` 颜色 token 在新建组件中的人体工程学。

**职责边界**（与 Tag 区分）：Badge 是**状态指示器**（5 固定 BadgeVariant level，颜色由 token 决定）。**负例**：不得接收 caller-defined palette（不得有 `Badge(color: Color)` overload）。

## Acceptance Criteria

### API 形态
- [ ] 新建 `Sources/CoreDesign/Components/Badge/Badge.swift`
- [ ] `public struct Badge<Label: View>: View`
- [ ] `public enum BadgeVariant { case info, success, warning, danger, neutral }` (Sendable)
- [ ] `public init(variant: BadgeVariant = .neutral, outlined: Bool = false, @ViewBuilder label: () -> Label)`
- [ ] `Badge(_ text: String, variant: BadgeVariant)` convenience init `where Label == Text`
- [ ] **不引入** `BadgeStyle` protocol（参考 Primer Label 的 parametric 形态；ADR #3）
- [ ] **不接收** `color: Color` 参数（per ADR / Tag 区分）

### 视觉
- [ ] 背景：`Color.surfaceCanvasSubtle`（neutral）/ `infoBackground` / `successBackground` / `warningBackground` / `dangerBackground`（按 variant）
- [ ] 边框（仅 `outlined: true` 时）：`Color.borderMuted` (neutral) / 对应 variant border color；宽度 `CoreBorderWidth.thin`
- [ ] 圆角：`CoreRadius.full`（pill 形态，per PRD FR-B-1）
- [ ] 字号：`CoreTypography.bodySmallFont` + `bodySmallTracking`（不引入新 tracking 字面量）
- [ ] padding：内边距走 `CoreSpacing.xs` / `sm`

### 跨任务 guardrail
- [ ] doc-comment 内显式标注 Badge 与 Tag 的区分边界（"状态指示器，5 固定 level；任意分类用 Tag"）

### Token-clean
- [ ] `Badge.swift` production 代码（非 #Preview）零 magic-numbers（NFR）

### Build / test / preview
- [ ] `swift build -Xswiftc -warnings-as-errors` 双平台通过 0 warning
- [ ] `swift test` 退出 0
- [ ] `#Preview` light / dark 双模式 5 个 BadgeVariant × outlined: true / false 共 10 种态视觉合理

### PR description 5 字段（按 epic ADR / SC#5）
- [ ] Light preview / Dark preview / API 摘要 / doc-comment 完整性勾选 / token-clean 自检

## Technical Details

参考 epic.md `### 简单结构组件`。本组件没有 `BadgeStyle` 协议（不像 Banner），是 parametric struct + enum——更轻量、更贴 Primer Label 形态。

可暴露 internal helper（`private static func backgroundColor(for: BadgeVariant) -> Color` 等）；这些 helper 不算 public API 表面，无 doc-comment 治理要求。

## Dependencies

- v2-tokens 全部 merged（已满足）

## Effort Estimate
- Size: S
- Hours: 4（0.5 天）

## Definition of Done

- [ ] Acceptance Criteria 全部勾选
- [ ] PR opened with 5 字段 description
- [ ] PR review + merge 到 main
