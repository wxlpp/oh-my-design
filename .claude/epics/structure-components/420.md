---
name: Timeline 改组合式 API（破坏性变更）
status: closed
created: 2026-09-23T01:18:58Z
updated: 2026-09-24T13:19:59Z
github: https://github.com/wxlpp/oh-my-design/issues/420
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Timeline 改组合式 API（破坏性变更）

契约见 `.claude/prds/timeline-tree-action-buttons.md` 的 **FR-1**（六项要求 a–f）；完整要点与迁移面见 issue #420。
现状盘点见同目录 `repo-survey.md` A 节；reui 的子组件分解见 `reference-implementations.md` 第 1 节。

⚠️ 迁移面里最容易漏的是 `QuotedEvidenceGuard` 登记的 5 条 Timeline 源码原文（删符号会判红，三份活文档要同步改）。

## Definition of Done
- [ ] 判据可被变异打红，且变异**不照着判据的形状构造**；变异后先确认它真的落到了文件里
- [ ] Reduce Motion 有分支且有判据（若涉及动效）
- [ ] 公开 API 无 Bool 入参；文案参数走 `LocalizedStringKey`
- [ ] 新组件登记 `docs/component-registry.json` + `docs/components/<name>.md`；**逐项裁决**是否进 `ComponentExtensionPointGuard` 定义域（当前逐字 `== 16`）
- [ ] 六条强制检查全绿：macOS `swift test`、iOS `xcodebuild`（取 `.xcresult` **顶层**计数）、预览宿主、`downstream-probe`、MainActor 棘轮、`design-digest.py`
- [ ] 行为 / API 变化登记 `docs/BREAKING-CHANGES.md`；docs 与 registry 的 `notes` 同步（「更正传播」三处落点）
