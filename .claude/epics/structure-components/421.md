---
name: CheckBox 增读系统 mixed 态（Tree 三态的前置）
status: closed
created: 2026-09-23T01:18:58Z
updated: 2026-09-23T03:09:17Z
github: https://github.com/wxlpp/oh-my-design/issues/421
depends_on: []
parallel: true
conflicts_with: []
---

# Task: CheckBox 增读系统 mixed 态（Tree 三态的前置）

契约见 `.claude/prds/timeline-tree-action-buttons.md` 的 **FR-2**「复选框含父节点三态」；完整要点见 issue #421。

⚠️ 不是造新模型：`ToggleStyleConfiguration.isMixed` 自 iOS 16 / macOS 13 即存在。
本任务只做 `CheckBoxToggleStyle.makeBody` 增读 `configuration.isMixed` 并呈现第三种符号；旧外观逐像素不变。

## Definition of Done
- [ ] 判据可被变异打红，且变异**不照着判据的形状构造**；变异后先确认它真的落到了文件里
- [ ] Reduce Motion 有分支且有判据（若涉及动效）
- [ ] 公开 API 无 Bool 入参；文案参数走 `LocalizedStringKey`
- [ ] 新组件登记 `docs/component-registry.json` + `docs/components/<name>.md`；**逐项裁决**是否进 `ComponentExtensionPointGuard` 定义域（当前逐字 `== 16`）
- [ ] 六条强制检查全绿：macOS `swift test`、iOS `xcodebuild`（取 `.xcresult` **顶层**计数）、预览宿主、`downstream-probe`、MainActor 棘轮、`design-digest.py`
- [ ] 行为 / API 变化登记 `docs/BREAKING-CHANGES.md`；docs 与 registry 的 `notes` 同步（「更正传播」三处落点）
