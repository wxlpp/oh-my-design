---
name: SlideToConfirm：滑到底才触发的高代价动作确认
status: open
created: 2026-09-23T01:18:58Z
updated: 2026-09-23T01:18:58Z
github: https://github.com/wxlpp/oh-my-design/issues/418
depends_on: []
parallel: true
conflicts_with: []
---

# Task: SlideToConfirm：滑到底才触发的高代价动作确认

契约见 `.claude/prds/timeline-tree-action-buttons.md` 的 **FR-4**；完整要点见 issue #418。
调研见 `.claude/epics/structure-components/reference-implementations.md` 第 4 节。

承重的三点：阈值**纯距离**（显式不采先例的速度补偿，理由要写进源码注释与文档）；
验收**必须含四条负例**；`.accessibilityRepresentation` 会**完全替换**无障碍表示 ⇒ 替代 `Button` 要自带名称 / 状态 / 禁用语义。

## Definition of Done
- [ ] 判据可被变异打红，且变异**不照着判据的形状构造**；变异后先确认它真的落到了文件里
- [ ] Reduce Motion 有分支且有判据（若涉及动效）
- [ ] 公开 API 无 Bool 入参；文案参数走 `LocalizedStringKey`
- [ ] 新组件登记 `docs/component-registry.json` + `docs/components/<name>.md`；**逐项裁决**是否进 `ComponentExtensionPointGuard` 定义域（当前逐字 `== 16`）
- [ ] 六条强制检查全绿：macOS `swift test`、iOS `xcodebuild`（取 `.xcresult` **顶层**计数）、预览宿主、`downstream-probe`、MainActor 棘轮、`design-digest.py`
- [ ] 行为 / API 变化登记 `docs/BREAKING-CHANGES.md`；docs 与 registry 的 `notes` 同步（「更正传播」三处落点）
