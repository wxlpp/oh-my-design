---
name: Badge / Tag / Avatar 尺寸体系 + AvatarSize
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:22:20Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Badge / Tag / Avatar 尺寸体系 + AvatarSize

## Description

`Badge` / `Tag` 跟随 `\.controlSize`；`CoreControlMetrics` 新增五档头像直径表，`Avatar` 新增 `size: AvatarSize`（`.automatic` / `.fixed`），`AvatarGroup` 共享同一张表。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-7 为准。

## Acceptance Criteria

- [ ] 五档 `ControlSize` 下三件渲染尺寸单调变化（测试断言尺寸）
- [ ] `Tag` 关闭钮随档
- [ ] `AvatarGroup` 内 `Avatar` 与组尺寸同源
- [ ] `Avatar(name:size: .fixed(100))` 可得任意直径；默认固定直径的布局破坏登记进 BREAKING-CHANGES
- [ ] 尺寸取值全部来自 `CoreControlMetrics`，组件内无硬编码数字

## Technical Details

008 依赖本 task（同触 `Tag.swift`）。

## Dependencies

无前置 task。

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
