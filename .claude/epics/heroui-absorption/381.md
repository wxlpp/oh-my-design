---
name: coreCircular 进度环 + 按压反馈 ButtonStyle
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:25:02Z
github: https://github.com/wxlpp/oh-my-design/issues/381
depends_on: []
parallel: true
conflicts_with: []
---

# Task: coreCircular 进度环 + 按压反馈 ButtonStyle

## Description

新增 `.coreCircular` `ProgressViewStyle` 与 `.pressableRow` / `.pressableCard` 两个 `ButtonStyle`。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-10、FR-11 为准。

## Acceptance Criteria

- [ ] `.coreCircular` 确定态画圆弧、颜色走 `.tint`（`TintShapeStyle`），`value == nil` 回退系统 spinner
- [ ] `.pressableRow` 按下铺 `pressedBackground`；`.pressableCard` 按下按 `pressedScale` 缩放，reduce motion 下只变暗
- [ ] 两者尊重 `\.isEnabled`，只装饰 label、不接 role / 不改布局，文档写明对《按钮样式模式》的有意例外
- [ ] `Color.pressedBackground` 在主 target 生产代码有消费

## Technical Details

style 实现不进组件登记表（公约）。

## Dependencies

无前置 task。

## Effort Estimate

- Size: S
- Hours: 6

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
