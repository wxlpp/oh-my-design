---
name: StatusLevel.neutral（原子，含全部消费者）
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:22:20Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: StatusLevel.neutral（原子，含全部消费者）

## Description

`StatusLevel` 新增 `.neutral`，同一改动内完成所有 exhaustive switch 消费处的外观与无障碍文案。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-4 为准。

## Acceptance Criteria

- [ ] `git grep` 出的每个 `StatusLevel` switch 都有 neutral 分支（Banner、Toast、Timeline 至少）
- [ ] neutral 取 content / fill 语义 token，不取资源色
- [ ] Timeline 的 neutral 无障碍文案已定义
- [ ] BREAKING-CHANGES 登记下游 exhaustive switch 的源码破坏

## Technical Details

先 `git grep -n 'StatusLevel\|case .info\|case .danger' Sources` 列全消费者，再改。004 / 005 依赖本 task 合入。

## Dependencies

无前置 task。

## Effort Estimate

- Size: S
- Hours: 4

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
