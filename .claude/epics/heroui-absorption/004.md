---
name: Banner 补齐（title / actions / dismiss）
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:22:20Z
github: (will be set on sync)
depends_on: [003]
parallel: true
conflicts_with: []
---

# Task: Banner 补齐（title / actions / dismiss）

## Description

`BannerStyleConfiguration` 增 `title` / `actions` / `dismiss`，新增便利 init，两个内建 style 渲染新槽，动作与关闭按钮成为独立可访问节点。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-5 为准。

## Acceptance Criteria

- [ ] `Banner(level:title:message:actions:onDismiss:)` 渲染标题、正文、动作、关闭钮
- [ ] configuration.label 语义不变（正文槽）
- [ ] 动作 / 关闭按钮可独立聚焦与激活，Banner 不再把全部子元素合并
- [ ] `onDismiss` 只回调，Banner 无状态
- [ ] BREAKING-CHANGES 写明自定义 `BannerStyle` 迁移

## Technical Details

只改 `Banner.swift` 及其文档 / 预览，与 005 可并行。

## Dependencies

依赖 003 合入 epic 分支。

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
