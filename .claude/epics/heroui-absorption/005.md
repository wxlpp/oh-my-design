---
name: Toast 补齐（API、ToastAction、ToastDuration、状态机）
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:22:20Z
github: (will be set on sync)
depends_on: [003]
parallel: true
conflicts_with: []
---

# Task: Toast 补齐（API、ToastAction、ToastDuration、状态机）

## Description

按 PRD FR-6 改造 Toast：`message → title`、`description`、`ToastAction`、`ToastDuration`、`dismissAll()`，并重写计时状态机。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-6 为准。

## Acceptance Criteria

- [ ] `ToastItem` 保持 `nonisolated` + `Sendable`；`ToastAction` Sendable，动作在主 actor 执行（后台构造、主线程展示与执行有测试）
- [ ] 点动作 = 执行后关闭；有动作时动作与关闭可独立聚焦
- [ ] 按住或拖拽暂停、松手按剩余时长恢复、手势取消同样恢复（有测试）
- [ ] `.persistent` 在关闭前阻塞队列；`dismissAll()` 在退场动画中也成立，之后立即 `show` 正常显示（有测试）
- [ ] 显示计时与退场等待不共用同一任务字段
- [ ] 三种 presentation 与 AX5 下动作不截断
- [ ] BREAKING-CHANGES 给出新旧签名映射

## Technical Details

只改 `Toast.swift` 及其文档 / 预览 / 测试。状态机建议抽成可注入时钟的内部类型以便测试。

## Dependencies

依赖 003 合入 epic 分支。

## Effort Estimate

- Size: L
- Hours: 16

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
