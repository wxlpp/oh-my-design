---
name: surface 有效层级 + coreSheetPresentation
status: open
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T01:25:02Z
github: https://github.com/wxlpp/oh-my-design/issues/382
depends_on: []
parallel: true
conflicts_with: []
---

# Task: surface 有效层级 + coreSheetPresentation

## Description

`.surface(_:)` 写入有效层级环境值并按层级取背景；新增 `View.coreSheetPresentation()`。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-12、FR-13 为准。

## Acceptance Criteria

- [ ] 层级规则按 PRD FR-12 逐角色表：canvas/canvasSubtle 重置、仅 content/grouped/card 父层级 +1 封顶且背景随层级变、panel/sidebar/control/floating 不改层级保持现背景（测试）
- [ ] `.surface(.content)` 内的 `Card` 取 `surfaceElevated`；描边规则仍由角色决定
- [ ] macOS grouped 嵌套无区分登记为已知限制
- [ ] `coreSheetPresentation()` 不设圆角（交给系统）、显示拖拽指示条、背景 `.system`（缺省）/ `.raised`，并把 sheet 内层级设为 raised
- [ ] ~~`CoreRadius.xLarge` 在主 target 生产代码有消费~~（PRD FR-13 修订后作废：sheet 圆角交给系统，见 37e7b8e）

## Technical Details

`Card` 保持 `.surface` 薄封装，不另写逻辑。

## Dependencies

无前置 task。

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
