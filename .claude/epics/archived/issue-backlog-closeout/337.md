---
name: 裸行号引用守卫（形态 2 引文逐字核）+ 129 条失真修复
status: closed
created: 2026-09-14T00:33:25Z
updated: 2026-09-14T02:18:16Z
github: https://github.com/wxlpp/oh-my-design/issues/337
depends_on: []
parallel: true
conflicts_with: [312]
---

# 任务：#337 散文行号引用无机器兜底

规格见 issue 正文 + 两条评论。**普查已做完**（`main @ a4afd16`：160 条裸行号引用，
129 失真 / 18 仍成立 / 13 跨仓；129 条中 77 纯漂移 / 26 整段被删 / 25 重构顶掉 / 1 抄错）。
开工时在现 main 上复跑普查拿**逐条清单**（评论里只有汇总数，没有条目表）。

## 范围

1. 复跑普查，产出逐条清单（文件 / 引用 / 档位 / 修法）。
2. 修 129 条：77 纯漂移（被引文字还在）→ 按形态 2 改写为可核形式；26 整段被删 →
   删除或换仍存在的证据；25 重构顶掉 → 按现状重写；1 抄错 → 改正。13 条跨仓如实登记。
3. ⚠️ 修正 `docs/contract-defects.md:1436` 被 `5241175` 推翻的 J-2 描述
   （现 `ComponentJudgeRules` 四分支：`nativeProtocol` / `customStyleProtocol` /
   `styleSlot` / `styleEnum`）——这条直接关系 `#312` 的形态 D 约束，优先修。
4. 落地形态 2 守卫：散文引用源码须带被引原文，判据核原文仍在文件中（复用
   `#316` `SingleSourceOfTruthGuard` 的 `factSites` 形态：显式登记 + 逐条回扫）。
5. 变异实证：**换族攻击**（遮蔽、等价拼写、被钉行之外加内容），不得照着判据形状构造。

## 已知坑

- 登记表的 `notes` 两族各由 `ExtensionEntryPointGuard` / `ComponentRegistryGuard` 管，
  都只查长度——本任务的守卫是第三族，注意与它们不重复也不打架。
- 判据按「名字 / 整段逐字」匹配，别按「行号 / 形状」匹配（行号正是本任务要消灭的）。
- 改 `docs/` 会顶动本仓在看的文档判据（`AgentGuideSyncGuard` 等），每次改完跑一遍
  相关 suite 再下结论。
