---
name: anchoredBadge modifier
status: closed
created: 2026-09-22T01:22:20Z
updated: 2026-09-22T06:26:30Z
github: https://github.com/wxlpp/oh-my-design/issues/379
depends_on: []
parallel: true
conflicts_with: []
---

# Task: anchoredBadge modifier

## Description

新增 `View.anchoredBadge(_:placement:)` 与 `AnchoredBadgeContent`（dot / count(max:) / text），贴在宿主角上。

契约以 `.claude/prds/heroui-absorption.md` 的 FR-8 为准。

## Acceptance Criteria

- [ ] `.count(120, max: 99)` 显示 `99+`，count 为 0 不显示
- [ ] `.dot` 显示红点；位置由枚举决定
- [ ] 取色 `statusDangerEmphasis` + `contentOnEmphasis`，不跟随 accent
- [ ] 计数并入宿主可访问值

## Technical Details

按 modifier 归属公约登记，不进 `components[]`（以公约判定为准）。

## Dependencies

无前置 task。

## Effort Estimate

- Size: S
- Hours: 5

## Definition of Done
- [ ] 按 PRD 对应 FR 的契约实现；公开 API 显式 `public`、无 Bool 入参
- [ ] 测试覆盖本 task 的验收项（行为断言，不是 grep）；macOS `swift test` 与 iOS Simulator xcresult 全绿
- [ ] `docs/components/*.md`、`docs/BREAKING-CHANGES.md`（如有破坏）、登记表与守卫台账按真实增量同步
- [ ] 预览宿主加入新组件 / 新状态并构建通过；`scripts/downstream-probe` 构建通过；MainActor 棘轮豁免数不增
- [ ] UI 改动：模拟器截图交视觉评审，无 Blocker
- [ ] 交付评审通过，PR 合入 `epic/heroui-absorption`
