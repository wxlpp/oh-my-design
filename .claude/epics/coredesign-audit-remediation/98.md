---
name: 测试质量重建 + Blossom 断言
status: closed
created: 2026-07-18T22:45:26Z
updated: 2026-07-21
github: https://github.com/wxlpp/CoreDesign/issues/98
depends_on: [93, 95]
parallel: true
conflicts_with: []
---

# Task: 测试质量重建 + Blossom 断言

## Description

把「96 tests passed」这个失真信号修正为可信信号，承载审计项 **C2、C4a、C4b、C5** 共 4 项（对应 FR-6 的测试部分）。

三件事：删除恒真断言（编译通过即必过的测试）、补上 Blossom trait 分流的真颜色断言、以及为 C5 的零测试目标产出**逐文件处置清单**。

**本任务只碰 `Tests/` 目录**，不改任何 `Sources/` 文件——这是它 `conflicts_with: []` 的原因，也是 owner 矩阵验算出 **007 ∩ 011 = ∅** 的基础，两者可真正并发（011 是机械清理，不改测试所依赖的行为）。

**布局断言层不在本任务**，由 004 随 Dynamic Type 改造一并编写——安全网必须与它守护的改动同批合入。本任务只负责恒真断言清理与 Blossom 分流断言。

## Acceptance Criteria

### C2 恒真断言清理

- [x] `ProgressIndicatorTests` —— 全文只有 `_ = ProgressIndicator()`，无任何 `#expect`：删除或改写为真行为断言
- [x] `FloatingGlassModifierTests` —— 用恒真的 `#expect(String(describing: type(of: view)).isEmpty == false)`：删除或改写
- [x] `StatusColorsTests` —— 5 个 test 共约 20 行，**没有一个 `#expect`**：删除或改写（注意 002 已先行让它编译通过，本任务做整体清理）
- [x] `SurfaceKindTests` —— 断言的是测试自己写的数组字面量长度，与被测代码无关：删除或改写
- [x] `AvatarTests` —— 断言 memberwise init 的赋值结果（编译器保证的事）：删除或改写
- [x] **保留**下列写得好的行为测试，且改动后仍全绿：`ToastHostTests`、`AsyncButtonTests`、`ProgressBarTests`、`ListRowTests`（其泛型 slot 断言是真行为断言）
- [x] 注意 `KeyboardHandlingTests` 已随 006 一并删除，**不在保留之列**，处置清单中按「已由 006 删除」记录

### C4a Blossom 分流断言

- [x] 新增至少 1 个测试能区分默认与 Blossom 主题的**实际颜色值**
- [x] **实现路径已实测确定，不得偏离**：`swift test` 下 asset 颜色**无法解析**（SwiftPM 不调用 `actool`，产出 bundle 只有原始 `.xcassets` 目录，`Color.accent.resolve()` 返回 `(0,0,0,0)`）。因此断言须走：
  1. `String(describing: Color)` 取 asset 名
  2. 解析对应 `.colorset/Contents.json` 的 sRGB 分量
  3. 按 `#if Blossom` 断言期望值
- [x] 期望值（取 light 值）：
  - 默认：`accent` → `brand-5` → `#0077FA`
  - Blossom：`accent` → `blossom-brand-5` → `#FF6F8E`
- [x] `swift test` 与 `swift test --traits Blossom` 两种模式下该测试**都跑且都绿**，且断言的期望值不同

### C4b asset guard 扩展

- [x] 现有 Blossom asset guard（`CoreDesignTests.swift:21-37`）扩展到覆盖分流同样依赖的 `violet-0…9` 与 `cyan-1`

### C5 覆盖缺口处置

- [x] 产出**逐文件测试处置清单**，同时覆盖 C2 与 C5 两份名单，每项标注「**本轮补测**」或「**记录不补 + 理由**」
- [x] C5 名单（约 15 个零测试目标）**不要求全部补测**：`CheckBoxToggleStyle`、`Form`、`CoreMenuButton`、四个 ButtonStyle、`ButtonRoleStyleRole`、全部 token 层（仅 `CoreButtonMetrics` 有测试）、全部 modifier、`StarShape`、`ColorExtension`
- [x] 清单作为 **C2 附录落盘到 `audit-checklist.md`**
- [x] **附录表格首列须用测试文件名**（如 `| ProgressIndicatorTests.swift |`），**不得**使用 `| C2a |` 这类 `^| [A-D][0-9]` 形态——否则会破坏 `audit-checklist.md` 顶部的计数核对命令：
  ```bash
  echo $(( $(grep -c '^| [A-D][0-9]' audit-checklist.md) - 4 ))   # 须仍 => 83
  ```

## Technical Details

### 为什么 C5 必须产出清单而非全部补测

若不产出逐文件处置清单，本任务做完也**无法在 `audit-checklist.md` 中标记 C5**，SC-7（83 项全部标记「已修复」或「记录不修 + 理由」）的对账会卡住。清单本身就是 C5 的交付物，也是 SC-5「恒真断言归零」可机械判定的前提——PRD 阶段无法逐一枚举 32 个 suite，枚举义务被下推到本任务。

### 依赖说明

- **002（真实文件依赖）**：`StatusColorsTests.swift` 同时是 002 的破坏对象（引用被删的 `statusAccent*` 与 legacy token）与本任务的改写对象（恒真断言）。**由 002 负责让它编译通过**（删掉引用已删 token 的断言），本任务再做恒真断言的整体清理。顺序颠倒会让 002 单独落地时 `swift test` 必红
- **004（语义依赖，无共享文件）**：等 004 的布局断言层落地后再定本任务的测试边界，避免两边重复或留下缺口

### 完成后的核对

改动后跑一次 `audit-checklist.md` 顶部两条核对命令，确认计数未漂移（83 / 79）。

### 代码风格

测试用 Apple 的 Swift Testing（`import Testing`、`@Test`、`#expect`），**不引入 XCTest**。不引入 `swift-snapshot-testing` 等外部测试依赖（Out of Scope）。

## Dependencies

- **前置任务**：002（负责让 `StatusColorsTests.swift` 重新编译通过）、004（语义依赖：布局断言层落地后再定测试边界）
- **可并行**：011（机械清理），矩阵验证 007 ∩ 011 = ∅
- **冲突**：无（本任务只碰 `Tests/`）
- **上游前提**：006 已删除 `KeyboardHandling.swift` 及 `KeyboardHandlingTests.swift`

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done

- [x] 4 个审计项（C2、C4a、C4b、C5）全部实施完成
- [x] 五个恒真断言测试文件全部删除或改写为真断言；四个保留的行为测试仍全绿
- [x] C4a 的 Blossom 分流断言在默认与 Blossom 两种模式下都跑、都绿，且断言值不同（`#0077FA` vs `#FF6F8E`）
- [x] 逐文件处置清单已作为 C2 附录落盘到 `audit-checklist.md`，首列为测试文件名
- [x] `audit-checklist.md` 顶部两条核对命令输出仍为 83 / 79（计数未漂移）
- [x] 四条 SwiftPM 命令全绿、零 warning：
  - [x] `swift build`
  - [x] `swift test`
  - [x] `swift build --traits Blossom`
  - [x] `swift test --traits Blossom`
- [x] 若本轮新增或删除了 colorset，先 `swift package clean` 再跑上述四条命令
- [x] 更新 `audit-checklist.md` 中 C2 / C4a / C4b / C5 四项的状态（SC-7 判定基础）
- [x] `git diff --name-only` 自查：改动全部落在 `Tests/` 与 `.claude/`（测试文件 + `audit-checklist.md` / `98.md` / `98-plan.md` / `updates/98/progress.md` 簿记），未触碰 `Sources/`
