---
name: 机械清理
status: closed
created: 2026-07-18T22:46:02Z
updated: 2026-07-21
github: https://github.com/wxlpp/CoreDesign/issues/102
depends_on: [93, 94, 95, 96, 97, 99, 100, 101]
parallel: true
conflicts_with: [93, 94, 95, 96, 97, 99, 100, 101]
---

# Task: 机械清理

## Description

本任务承载 10 个审计项：**C6a, C6b、C10a、C10c、C10d、D9、D12、D15、D16a、D16b、D18**。它是原 #10 按第 2 轮评审建议二分后的后半段——前半段（010）是设计决策，本任务是**文本与机械改动**：目录归位、文档修正、gitignore、硬编码数值改 token、Preview 补全。二分的理由是评审粒度：把 LICENSE、gitignore 去重这类项与 style 协议化混在一个 Issue 里会让评审失配。

因为**几乎全是"在别人改完之后收尾"的动作**，本任务排在最后（epic 阶段 6），依赖面最广。**可与 007（测试质量重建）并行**——矩阵验证 `#11 ∩ #7 = ∅`，本任务是机械清理、不改测试所依赖的行为。

## Acceptance Criteria

### 目录与仓库卫生

- [ ] `Banner.swift` 移入 `Components/Banner/Banner.swift`，与其余组件的 `Components/<Name>/<Name>.swift` 布局一致（D9）
- [ ] 补 `LICENSE`（C10a）
- [ ] `.gitignore` 去重——`.superpowers/` 目前重复出现两次（C10c）
- [ ] 消除 `.claude/`、`.agents/`、`AGENTS.md` 的悬空状态（既未 ignore 也未 tracked）（C10d）：
  - `.claude/prds/` 与 `.claude/epics/` **纳入版本控制**（本 epic 的 artifact 存放处）
  - `.agents/` 与 `.claude/omsp/` 加入 `.gitignore`
  - `AGENTS.md` 纳入版本控制
- [ ] README 组件数量修正——现称「15 documented components」，实际 **24 个组件目录 / 26 个 Preview**（C6b）

### 硬编码数值改引用 token（D12）

- [ ] `CoreMenuButton.swift:138` 的 `scaleEffect(0.94)` → `CoreButtonMetrics.pressedScale`（数值恰好相同）
- [ ] `CoreMenuButton.swift:128` 的 `HStack(spacing: 8)` → `CoreSpacing.sm`
- [ ] `TimelineItem.swift:74` 的 `VStack(spacing: 0)` → `CoreSpacing.none`
- [ ] `TimelineItem.swift:99-104` 的裸 dotSize `32` / `20` 改引用 token 或提为命名常量
- [ ] `CommentCard.swift:59` 的 `padding(.vertical, 1)` —— **`1` 不在任何 spacing 档位内**，须判定是改到最近档位还是提为命名常量，并在任务记录中写明理由
- [ ] `BottomInputBar.swift:219` 的 `44` 提为**命名常量**（HIG 最小可点击区域魔法数）。**注意：不能替换为 `CoreControlMetrics.height(for: .extraLarge)`，该值是 48**
- [ ] `AvatarGroup.swift:33-40,76-85` 的硬编码数值改引用 token

### 一致性与文档漂移

- [ ] `FillColors.swift:16-23,30-37,44-51,58-65` 四处平台分支改 `#if / #else` 形式，与同层两个桥接文件（`SystemBackgroundColors`、`SystemLabelColors`）写法一致——当前缺 `#else`，两条件皆不成立时无 return（D15）
- [ ] `StateLabel.swift:28` 的文档注释只列 4 个 style，而枚举（`:14-21`）实际有 6 个，须补全（D16a）
- [x] ~~CLAUDE.md 修正 `.focusedExternally` 的描述~~ —— **已由 #97 顺带完成**（Task 1 删 `.getSize` 时重写了《Modifier 约定》整句）。原文：它不是 `Utils/` 下的通用辅助，而是 `BottomInputBar.swift` 的 private extension（D16b）

### Preview 补全（D18）

- [ ] `Sidebar.swift`（391 行 / 6 个 public 组件）补 `#Preview`——**全库最大的视觉冒烟缺口**
- [ ] `FloatingGlassModifier.swift` 补 `#Preview`（public 且无 Preview）
- [ ] `TelegramGlassButtonModifier.swift` 补 `#Preview`（同上）

## Technical Details

### CLAUDE.md 由本任务统一改一次（关键协调点）

CLAUDE.md 被 **002**（分层色彩描述随 `FunctionalColor` 重定义更新）、**006**（删去 `.getSize` 的通用辅助描述）、**011**（D16b 的 `.focusedExternally` 描述）**三方触碰**。epic Implementation Strategy 与 PRD 冲突表都建议**由本任务统一改一次**，避免三次编辑同一文件产生冲突。

因此本任务须核对：002 与 006 应留下的 CLAUDE.md 改动是否已落地；若未落地，一并在此改完，并在任务记录中列出「代 002 / 006 改了什么」。

### `Banner.swift` 的移动 vs 编辑冲突（D9）

`Banner.swift` 被 **002**（legacy status token 迁移）、**004**（`CoreTypography` 消费）、**010**（B8b `makeBody` 抽公共实现、B8e `StatusLevel` 合并）触碰，而本任务要**整文件移目录**。**移动 vs 编辑是所有冲突类型里最烈的一种**——git 会把它呈现为「删除 + 新增」，其它任务在旧路径上的改动会静默丢失。

约束：**本任务的 D9 必须在其它任务改完 `Banner.swift` 之后执行**，这正是 `depends_on` 含 002/004/010 的原因。移动时用 `git mv` 保留历史。

### `BottomInputBar.swift:219` 的 `44` 不可直接替换（易错点）

`44` 是 HIG 最小可点击区域的经典魔法数。**`CoreControlMetrics.height(for: .extraLarge)` 是 48，不是 44**——直接替换会静默改变布局。正确处置是提为命名常量（如 `minimumHitTargetSide`），并在注释里写明它是 HIG 值而非 metrics 序列内的档位。

### `Sidebar.swift` 的额外验证要求（关键）

D18 要给 `Sidebar.swift` 补 `#Preview`，而 **004 在该文件落了 Dynamic Type 布局断言**。该断言被 `#if os(iOS)` 包住，**四条 SwiftPM 命令下完全不可见**。

因此本任务**必须额外跑第 5 条验证命令**：

```bash
xcodebuild test -scheme CoreDesign -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

epic「每个 Issue 的完成定义」第 4 条明确点名本任务与 010 两者。若 001 的 CI 降级到本地 pre-push 级别，这条命令须在本地手工执行并记录输出。

### Preview 是本仓库的主要视觉冒烟手段

CLAUDE.md 明确：`#Preview` 块与对应组件放在**同一文件**中，它们不是测试，但是组件的主要视觉冒烟检查方式。补 Preview 时须与仓库既有形态一致（同文件、覆盖主要配置组合），`Sidebar` 的 6 个 public 组件应各有可辨识的预览场景而非单一堆叠。

### 触及文件

`Components/Banner/Banner.swift`（移动）、`CoreMenuButton`、`TimelineItem.swift`、`CommentCard.swift`、`BottomInputBar.swift`、`AvatarGroup.swift`、`FillColors.swift`、`StateLabel.swift`、`Sidebar.swift`、`FloatingGlassModifier.swift`、`TelegramGlassButtonModifier.swift`、`CLAUDE.md`、`README.md`、`.gitignore`、`LICENSE`（新增）。

## Dependencies

- **前置**（真实文件级，本任务是全 epic 依赖面最广的）：
  - **002** —— `Banner.swift`（legacy token 迁移）、CLAUDE.md 分层描述
  - **003** —— `MenuButton` → `CoreMenuButton` 改名，D12 的两处硬编码在该文件
  - **004** —— 多个共享文件（`TimelineItem` / `CommentCard` / `AvatarGroup` / `Sidebar` / `Banner`）+ `Sidebar` 的布局断言
  - **005** —— `Sidebar` row 骨架收敛，D18 的 Preview 须基于收敛后的形态
  - **006** —— `TimelineItem`（`@Entry` 改造）、`CommentCard`（surface 三件套）、`BottomInputBar`（B8h 拆分）、CLAUDE.md 的 `.getSize` 删除
  - **008** —— `BottomInputBar`（可访问性标签）
  - **009** —— `CoreMenuButton` / `BookCover` 的字符串改动
  - **010** —— 共享 7 个文件（`Banner` / `TimelineItem` / `CommentCard` / `AvatarGroup` / `Sidebar` / `StateLabel` 等）
- **可并行**：**007**（测试质量重建）——矩阵验证 `#11 ∩ #7 = ∅`。本任务是机械清理、不改测试所依赖的行为，007 只动测试文件，无需互等
- **冲突**：002、003、004、005、006、008、009、010

## Effort Estimate

- Size: M
- Hours: 12

10 项但每项都是机械改动（无设计决策），单项工作量小。工作量集中在三处：`Sidebar` 的 6 个 public 组件补 Preview（最大的一块）、CLAUDE.md 代 002/006 统一改动的核对、以及 `Banner.swift` 移目录后的引用与 docs 收口。另有两处需判断而非机械替换（`CommentCard` 的 `1` 与 `BottomInputBar` 的 `44`）。

## Definition of Done

- [ ] `swift build` 绿、零 warning
- [ ] `swift test` 绿、零 warning
- [ ] `swift build --traits Blossom` 绿、零 warning
- [ ] `swift test --traits Blossom` 绿、零 warning
- [ ] **第 5 条命令绿**：`xcodebuild test -scheme CoreDesign -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`——本任务的 D18 给 `Sidebar.swift` 补 Preview，而 004 的布局断言 `#if os(iOS)` 包住，前四条命令下不可见
- [ ] 若本任务新增或删除了 colorset（预期不会），须先 `swift package clean` 再跑上述验证
- [ ] `audit-checklist.md` 中 C6b、C10a、C10c、C10d、D9、D12、D15、D16a、D16b、D18 十项的状态已更新（SC-7 判定基础）
- [ ] CLAUDE.md 的三方触碰已统一收口：记录本任务代 002 / 006 改了哪些段落（若它们已自行改完则记录「已核对无遗漏」）
- [ ] `Banner.swift` 用 `git mv` 移动、历史保留，且全仓无残留旧路径引用（含 docs）
- [ ] 两处需判断的数值（`CommentCard.swift:59` 的 `1`、`BottomInputBar.swift:219` 的 `44`）处置理由已写入任务记录
- [ ] 三个新增 `#Preview` 已在 Xcode 中实际渲染确认（Preview 是本仓库主要视觉冒烟手段，编译通过不等于渲染正常）
