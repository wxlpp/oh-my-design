---
name: 死代码清理与现代化
status: closed
created: 2026-07-18T22:45:26Z
updated: 2026-07-20
github: https://github.com/wxlpp/CoreDesign/issues/97
depends_on: [93, 94]
parallel: true
conflicts_with: [93, 94, 95, 99, 100, 101, 102]
---

# Task: 死代码清理与现代化

## Description

删除全库死代码并把若干过时写法现代化，承载审计项 **B4a、B4b、B4c、B4d、B7a、B7b、B7c、B8c、B8h、B9a、B9b、B9c、B9d、B9f、B9g、D8、D10** 共 **17 项实施**（原列 18 项，其中 **B8d 于执行期实测撤销**——前提不成立，理由见 Acceptance Criteria）——是全 epic 承载项最多的任务（对应 FR-5 全部 + FR-4 的 B8h）。

触及约 **16 个 swift 文件** + `CLAUDE.md` + 3 个 docs 文件，其中含**三个整文件删除**及其测试。

本任务属于 epic 的**并行窗口 1**：owner 矩阵逐对验算确认 **005 ∩ 006 = ∅**，可与 005（按钮 + Sidebar 收敛）真正并发。因为本任务承载项最多，这一段并行对总工期收益最大。

本任务排在 004（Dynamic Type）**之前**是刻意安排——否则 004 会白白迁移即将被整文件删除的 `EmptyState.swift`。

## Acceptance Criteria

### 三个整文件删除

- [ ] **B9g**：删除 `Components/EmptyState/EmptyState.swift` **整文件**（237 行，已全文件废弃，同一 deprecated message 在 `:54,141,153,182,203,216` 重复 6 次）及其自证测试 `EmptyStateDeprecationTests.swift`
- [ ] **B4a + B4b**：删除 `BottomInputBar.swift:87,138` 的 `textFieldSize`（写入后从不读取，每次布局白跑 GeometryReader + PreferenceKey 往返），随后删除 `Utils/View+SizeReader.swift` **整文件**（51 行；`getSize` 全库唯一消费点即 `BottomInputBar.swift:138`，删后整文件成死代码）
- [ ] **B4c + B4d**：删除 `Utils/KeyboardHandling.swift` **整文件**（含 `KeyboardReadable` 及其默认实现、`dismissKeyboardOnTap`、`resignFirstResponder`、`anyWriterFirstResponderNotification` / `becomeFirstResponder`）**及其测试 `KeyboardHandlingTests.swift`**
  - 不得只删 B4c 的零引用部分而保留 `KeyboardHeightPublisherFactory`——它无生产调用点、仅被该测试消费，保留即构成「只为测试而活」的死代码，正是本 epic 要清理的东西

### 文档同步（删除的连带义务）

- [ ] 从 `CLAUDE.md` 删去 `.getSize` 的「`Utils/` 通用辅助方法」描述
- [ ] 从 `docs/README.md:41` 移除 EmptyState 的组件索引行
- [ ] 删除 `docs/components/empty-state.md` 迁移文档

### CoreGradient

- [ ] **B7b**：三个 `static var`（`CoreGradient.swift:21,37,53`）改 `static let`，消除每次求值新建 `AnyShapeStyle` box、放 body 里每帧重新装箱的开销
- [ ] **B7c**：文件从 `Colors/` 移入 `Tokens/`，与 CLAUDE.md「渐变 token 层」的定位一致
- [ ] **B7a**：至少一处组件**真实消费** `CoreGradient`（当前全仓零采用，仅自身 Preview + 一个空测试），以验证该抽象成立
  - **消费点必须钉在本任务自有文件内：`BookCover` 或 `CommentCard` 二选一**。若选中 005 的文件（如某个 ButtonStyle）会**静默破坏 005 ∩ 006 = ∅ 的并行前提**——这是 epic 已识别的矩阵盲点，执行时不得偏离

### 组件现代化

- [ ] **B8c**：`CommentCard.swift:93-101` 手写的 surface 三件套（逐 token 与 `.surface(.card)` 一致）改为 `.surface(.card)`
- [x] ~~**B8d**：`RefPill.swift:51-56` 同型手写 surface 三件套同样收敛~~ —— **撤销（#97 执行期实测，经用户确认）**。该项的前提「与 `CommentCard` 同型」不成立：`RefPill` 用 `surfaceCanvasInset` + `borderMuted` + `CoreRadius.small` + 默认 `.circular` 圆角，而**没有任何 `SurfaceKind` 的 background 是 `surfaceCanvasInset`**（`.control` = `surfaceInteractive`/`borderSubtle`、`.canvasSubtle` = `surfaceCanvasSubtle`/`.medium`），且 `SurfaceModifier` 一律用 `.continuous` 圆角。换任何现有 kind 都会同时改变背景色、圆角半径、圆角曲线三项——是视觉回归而非重复消除。`surfaceCanvasInset` 的取色与 pill「嵌在正文中的引用标记」定位相符，判定为有意而非疏忽。若日后仍要收敛，需先给 `SurfaceKind` 加 `.inset` case（属加法，本 epic Out of Scope）
- [ ] **B8h**：`BottomInputBarModifier.body`（`BottomInputBar.swift:361-438`，78 行，全库唯一超 50 行的 body）拆分子视图；`:416-437` 两个同构 `onChange` 合并为单一 `syncSuggestionsVisibility(shouldShow:)`；`:297-308,374-381` 重复的 chip 样式收敛
- [ ] **B9a**：`BookCover.swift:74,95-106` 的 `UIImage` / `NSImage` 解码移出 body（当前列表滚动时反复解码）
- [ ] **B9b**：`TimelineItem.swift:10-19` 手写的旧式 `EnvironmentKey`（10 行）改为 `@Entry`（3 行，同仓既有写法）；**须同步改 `TimelineItemTests.swift:30`**
- [ ] **B9c + D8**：删除 `BorderModifier.swift:26-33` 的 `bordered(color:)` 死重载（两重载全默认参数导致裸写 `.bordered()` 构成歧义）；`BorderModifier.swift:20` 从 `stroke` 改为全仓约定的 `strokeBorder`，并支持任意 shape（当前写死 `RoundedRectangle`，`cornerRadius: 0` 时误导且无法用于 `Capsule`）
- [ ] **B9d**：删除 `SegmentedControl.swift:205,301` 恒真的 `@available(iOS 26.0, *)`（部署目标已 iOS 26+）
- [ ] **B9f**：删除 `CheckBox.makeBody`（`CheckBox.swift:32`）上冗余的 `@MainActor @preconcurrency`（协议声明已携带隔离）
- [ ] **D10**：删除死 token `CoreRadius.full = 9999`（`CoreRadius.swift:59`；所有 pill 场景都用 `Capsule()`）

## Technical Details

### 删除的验证方式

每个整文件删除后须 grep 确认零残留引用，不能只靠编译通过——公开符号被删除时下游不在本仓，编译器不会报错。删除清单：

| 删除对象 | 唯一消费点 / 依据 |
|---|---|
| `EmptyState.swift` + `EmptyStateDeprecationTests.swift` | 全文件已 deprecated，测试是自证测试 |
| `View+SizeReader.swift` | `getSize` 唯一消费点 `BottomInputBar.swift:138`（本任务同时删除） |
| `KeyboardHandling.swift` + `KeyboardHandlingTests.swift` | 生产调用点为零；`KeyboardHeightPublisherFactory` 仅测试消费 |

### 与 007 的交接

`KeyboardHandlingTests.swift` 随本任务删除，**不在 007 的「保留测试」名单之列**。007 的逐文件处置清单须以此为前提。

### 依赖说明

- **002**：色彩层重组会改写 `CommentCard` / `RefPill` 等文件所依赖的语义 token，且 `CheckBox.swift` 是 002 与 003 的共享文件；本任务的 B9f 同样改 `CheckBox.swift`
- **003**：`CheckBox` 演示视图内联进 `#Preview`（B9e）由 003 完成，本任务的 B9f 须在其后

### 与 005 并行的硬约束

005 ∩ 006 = ∅ 是矩阵验算结论，**执行时不得破坏**：本任务不得触碰 005 的自有文件（四个 ButtonStyle、`ButtonRoleStyleRole.swift`、`Sidebar.swift`、`CoreMenuButton.swift`、`TelegramGlassButtonModifier.swift`）。B7a 的消费点选择是这条约束下最容易失手的一处，已在 Acceptance Criteria 中钉死。

### 代码风格

遵循仓库既有约定：显式 `self.`、中英双语注释与 `// MARK: -`、`#Preview` 与组件同文件。保留有设计说明价值的长注释（如 `BottomInputBar.autoFocus` 解释为何必须在 bar 自身 `onAppear` 中执行）——拆分 `body` 时不要连带丢弃。

## Dependencies

- **前置任务**：002（色彩层重组）、003（公开 API 修复与改名，共享 `CheckBox.swift`）
- **可并行**：005（按钮体系 + Sidebar 收敛），矩阵验证交集为空
- **冲突（须串行）**：002、003、004、008、009、010、011
- **下游**：004 排在本任务之后，避免迁移即将删除的 `EmptyState.swift`；008 依赖本任务先完成 `BottomInputBar` 的 body 拆分；007 的处置清单以 `KeyboardHandlingTests.swift` 已删除为前提

## Effort Estimate

- Size: XL
- Hours: 20

## Definition of Done

- [ ] 17 个审计项（B4a–d、B7a–c、B8c、B8h、B9a–d、B9f、B9g、D8、D10）全部实施完成；**B8d 撤销**（前提不成立，见 AC）
- [ ] 三个整文件删除完成，且各自的测试文件一并删除；grep 确认零残留引用
- [ ] `CLAUDE.md`、`docs/README.md`、`docs/components/empty-state.md` 三处文档同步完成
- [ ] B7a 的 `CoreGradient` 消费点落在 `BookCover` 或 `CommentCard`（`git diff --name-only` 自查，确认未触碰 005 的文件）
- [ ] 四条 SwiftPM 命令全绿、零 warning：
  - [ ] `swift build`
  - [ ] `swift test`
  - [ ] `swift build --traits Blossom`
  - [ ] `swift test --traits Blossom`
- [ ] 若本轮新增或删除了 colorset，先 `swift package clean` 再跑上述四条命令
- [ ] 更新 `audit-checklist.md` 中上述 17 项已修复 + B8d 撤销的状态（SC-7 判定基础）
- [ ] 视觉冒烟：`BookCover` / `CommentCard` / `RefPill` / `BottomInputBar` / `SegmentedControl` / `TimelineItem` 的 `#Preview` 在默认与 Blossom 两种模式下渲染正常，B7a 引入的渐变消费点观感符合预期
