---
name: 按钮体系 + Sidebar 收敛
status: closed
created: 2026-07-18T22:45:26Z
updated: 2026-07-20
github: https://github.com/wxlpp/CoreDesign/issues/96
depends_on: [94]
parallel: true
conflicts_with: [94, 95, 100, 101, 102]
---

# Task: 按钮体系 + Sidebar 收敛

## Description

把四个 ButtonStyle 与 `Sidebar` 四种 row 中的结构性重复收敛掉，承载审计项 **B2b、B3a、B3b、B3c、B3d、B3e、B5、B8a** 共 8 项（对应 FR-4 的按钮 / Sidebar 部分，外加 FR-3 声明但归本任务实施的 B2b）。

本任务属于 epic 的**并行窗口 1**：owner 矩阵逐对验算确认 **005 ∩ 006 = ∅**（触及文件交集为空），因此可与 006（死代码清理）真正并发执行。这一段并行直接缩短 epic 最长的关键路径。

本任务同时是 004（Dynamic Type 改造）排在其后的收益来源：`Sidebar.swift` 现有 **16 处 `CoreTypography.` 引用**，是全库最高的单文件消费者；骨架收敛后四个 row 的 body 内应从 11 处缩到 ≤8 处（另 5 处在 `SidebarSection` / `SidebarStatusFooter`，不在 B5 范围），004 的改动面随之下降。四个 ButtonStyle 的 font 行同理——B3d 把它们收敛成单一共享 modifier 后，004 的 `CoreControlMetrics.font(for:)` → `fontToken(for:)` 改造只需改一个调用点而非五个。

**B2b 的归属说明**：PRD FR-3 把「`Sidebar` 四处 `frame(height:)` → `minHeight`」写在 Dynamic Type 名下，但审计项 B2b 的实施**归本任务**，不归 004。理由是两者改**同一批行**（`Sidebar.swift:121,183,238,287`），由本任务在收敛出新骨架时一并带上，可彻底消除该处跨 Issue 文件冲突。004 只声明需求，不承担实施。

## Acceptance Criteria

- [ ] **B3a**：`ButtonRoleStyleRole` 新增 `resolvedColor(isEnabled:isPressed:)`，吸收 `SolidButtonStyle.swift:71-76`、`LightButtonStyle.swift:57-62`、`CoreBorderlessButtonStyle.swift:73-78` 三份逐字重复的三态取色逻辑；三个 style 内的私有取色实现删除，改为调用该方法
- [ ] **B3b**：`SolidButtonStyle.swift:38-65` 与 `LightButtonStyle.swift:27-51` 的 glass / 非 glass 分支不再整段复制——两者仅尾部 modifier 不同的部分抽出，共同结构只写一次
- [ ] **B3c**：`SolidButtonBackgroundModifier`（`SolidButtonStyle.swift:81-99`）与 `LightButtonBackgroundModifier`（`LightButtonStyle.swift:67-83`）合并为单一类型
- [ ] **B3d**：font / padding / contentShape 四行（在 4 个 style 里共出现 5 次，含 `CoreBorderlessButtonStyle.swift:43-46`）提炼为一个共享 `ViewModifier`，以 `View` 扩展形式暴露（遵循仓库 `Modifier/` 约定，不要求调用方写 `.modifier(...)`）
- [ ] **B3e**：`CircularGlassButtonStyle` 接入 `@Environment(\.controlSize)`，diameter 不再写死 38（`CircularGlassButtonStyle.swift:16,18`）；新的 diameter 序列取自 `CoreControlMetrics`，落在 metrics 既有序列内
- [ ] **B5**：`Sidebar` 四个 row（`Sidebar.swift:104-130,157-188,215-243,263-292`）收敛为**共享骨架 + 薄封装**
- [ ] **B2b**：`Sidebar.swift:121,183,238,287` 四处 `frame(height:)` → `minHeight`（与 `ListRow` / `SearchField` 的既有写法一致），随骨架收敛一并落地
- [ ] **B8a**：`CoreMenuButtonStyleModifier`（`CoreMenuButton.swift:87-118`）两分支的重复消除，且不再重写 `TelegramGlassButtonModifier.swift:47-63` 的同一结构——复用后者而非平行实现
- [ ] **SC-8**：四种 row + 共享骨架的 `var body` 之和 **≤ 100 行**（基线 118，口径与数字见下方《SC-8 的测量边界》）
- [ ] **四个 row 的 `var body` 内**的 `CoreTypography.` 引用从 **11 处降至 ≤8 处**（原 AC 写「文件级 16 → 约 6」不可达：16 处中有 5 处在本任务范围外——`SidebarSection` 的 `:49 :54 :64` 与 `SidebarStatusFooter` 的 `:330 :334`，B5 不碰这两个类型；且 6 在结构上到不了，四 row 的 leading/trailing 字号本就各不相同。#96 执行期实测修正，经用户确认）
- [ ] 四个 ButtonStyle 保持现有 `ButtonStyle` + `static func *Button(role:)` 静态扩展形态，**不协议化**（PRD FR-4 明确约束）
- [ ] 公开 API 表面无回退：改动涉及的类型 / init / 属性该 `public` 的仍 `public`

## Technical Details

### SC-8 的测量边界（须严格按此口径自查）

`SidebarNavigationRow` / `SidebarUtilityRow` / `SidebarDocumentRow` / `SidebarTagRow` 四个类型的 **`var body` 块之和**（`public var body: some View {` 那行到其配对 `}`，含首尾两行）：

- **含**共享骨架类型的 `body` 块
- **不含** `public init`、`private let` 成员声明、结构体声明/收尾行
- **不含** `#Preview` 与文档注释

**硬上限：TOTAL ≤ 100 行**（改造前基线 118）。完成时须在 PR 描述中给出实测行数与所用脚本。

> **两次修正（#96 执行期，2026-07-20，均经用户确认）。**
>
> **修正 1 · 测量边界。** 本节原写「**含**共享骨架类型与薄封装 init」。按该口径，四个 `public init`（各约 11 行）+ 14 个 `private let` + 结构声明/收尾行合计已约 79 行，**在写任何 body 之前就已超 60**。改为上面的 body-only 口径——证据：实测四个 row 的 body 块合计恰好 **118** 行，与 PRD 原话「约 120 行降至约 50 行」的前半吻合，且本节自身列出的 B5 坐标（`Sidebar.swift:104-130,157-188,215-243,263-292`）同样是 body-only 区间。
>
> **修正 2 · 上限数字 60 → 100。** 改完口径后实测仍不可达：把 `96-plan.md` 提议的骨架 + 四个薄封装落成代码跑同一脚本得 **99 行**。穷尽退路——删光 body 内全部 `//` 注释与空行得 85、连骨架 body 也排除得 68、两者都做得 63——**四种组合全部 > 60**。唯一能压进 60 的做法是把多行实参表与链式调用折成单行，即靠折行凑数，与「删注释凑数」性质相同。
>
> 结论：PRD 的「约 120 → 约 50」中，118 是实测、50 是拍的，从未对照真实设计验算。现按本次实测重定为 **≤100**（对应 99，留 1 行余量）。该指标的作用是给「收敛是否真的发生」一个机械判据并防止倒退，`118 → ≤100` 仍能守住这一点；把它降级为方向性指标等于废掉它，故不采用。

### 依赖 003 的原因

003 承载 `BorderlessButtonStyle` → `CoreBorderlessButtonStyle` 改名（A3a）与 `ButtonRoleStyleRole` 三个调色板属性补 `public`（A2c）、`MenuButton` → `CoreMenuButton` 改名（A3b）。本任务改写的正是这批文件的内部实现，须在改名与 `public` 修复落地后进行，否则 B3a 的 `resolvedColor` 会建立在即将被改的可见性之上。

### 与 004 的分工边界

- 本任务**不做** `CoreControlMetrics.font(for:)` → `fontToken(for:)` 的签名改造，那是 004 的范围
- 本任务**只**把散落的 font 行收敛到单一 modifier / 单一调用点，为 004 让路
- 若收敛过程中发现某处 font 调用必须同时改签名才能编译，停下并在任务中记录，不要越界改 `CoreControlMetrics` 的公开签名

### 与 006 并行的硬约束

005 ∩ 006 = ∅ 是矩阵验算结论，**执行时不得破坏**：本任务不得触碰 006 的自有文件（`EmptyState.swift`、`View+SizeReader.swift`、`KeyboardHandling.swift`、`BottomInputBar.swift`、`CommentCard.swift`、`RefPill.swift`、`SegmentedControl.swift`、`TimelineItem.swift`、`BookCover.swift`、`CoreGradient.swift`、`BorderModifier.swift`）。若确有必要，须先与 006 协调而非直接改。

### 代码风格

遵循仓库既有约定：显式 `self.`、中英双语注释与 `// MARK: -`、`#Preview` 与组件同文件、不为 iOS 26 API 加可用性回退。

## Dependencies

- **前置任务**：003（公开 API 修复与改名）—— 共享 `CoreBorderlessButtonStyle.swift`、`CoreMenuButton.swift`、`ButtonRoleStyleRole.swift`
- **可并行**：006（死代码清理与现代化），矩阵验证交集为空
- **冲突（须串行）**：003、004、009、010、011
- **下游**：004 依赖本任务先把 Sidebar 与 ButtonStyle 的 font 引用收敛

## Effort Estimate

- Size: L
- Hours: 12

## Definition of Done

- [ ] 8 个审计项（B2b、B3a–e、B5、B8a）全部实施完成
- [ ] SC-8 达标：body 之和 ≤ 100 行（按《SC-8 的测量边界》实测，PR 中给出数字与脚本）
- [ ] 四条 SwiftPM 命令全绿、零 warning：
  - [ ] `swift build`
  - [ ] `swift test`
  - [ ] `swift build --traits Blossom`
  - [ ] `swift test --traits Blossom`
- [ ] 若本轮新增或删除了 colorset，先 `swift package clean` 再跑上述四条命令
- [ ] 更新 `audit-checklist.md` 中 B2b / B3a / B3b / B3c / B3d / B3e / B5 / B8a 八项的状态（SC-7 判定基础）
- [ ] 未触碰 006 的自有文件（`git diff --name-only` 自查，确认 005 ∩ 006 = ∅ 仍成立）
- [ ] 视觉冒烟：`Sidebar` 与四个按钮样式的 `#Preview` 在默认与 Blossom 两种模式下渲染正常，观感无回归
