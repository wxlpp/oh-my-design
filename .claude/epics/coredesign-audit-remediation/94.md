---
name: 公开 API 修复与改名
status: closed
created: 2026-07-18T22:44:39Z
updated: 2026-07-20
github: https://github.com/wxlpp/CoreDesign/issues/94
depends_on: [93]
parallel: false
conflicts_with: [93, 96, 97, 100, 101, 102]
---

# Task: 公开 API 修复与改名

## Description

承载 6 个审计项：A2a、A2b、A2c、A3a、A3b、B9e（对应 FR-2）。

两条主线：

1. **补 `public`**——一批公开 API 因漏写 `public` 而下游不可用，已用下游消费包实测出编译错误（`cannot find in scope` / `initializer is inaccessible`），其中包括文档明确指引业务侧使用的 `CheckBoxToggleStyle`。
2. **改名避开 SwiftUI 同名冲突**——`BorderlessButtonStyle` 与 `MenuButton` 都与 SwiftUI 内建类型同名。这比漏写 `public` 更隐蔽：下游写这两个名字**能编译通过**，但静默解析到 SwiftUI 版本。

## Acceptance Criteria

- [ ] `CheckBoxToggleStyle` 补 `public`（含 init 与 `makeBody`），下游能成功构造（A2a）
- [ ] `BorderlessButtonStyle` → `CoreBorderlessButtonStyle`，补 `public init(role:)` 与 `public let role`（A2b、A3a）
- [ ] `MenuButton` → `CoreMenuButton`（A3b）
- [ ] `ButtonRoleStyleRole` 的 `color` / `activeColor` / `disabledColor` 三个调色板属性补 `public`（A2c）
- [ ] `FunctionalColor` extension 整层对下游可见（与 #2 的 A2d 协同，本任务复核 `success`/`info`/`warning`/`danger` 及变体确实 public）
- [ ] `CheckBox` 演示视图内联进 `#Preview`，只保留 `CheckBoxToggleStyle` 作为公开面（B9e）
- [ ] `App/` 预览宿主同步适配两处改名
- [ ] 下游 probe 包验证：0 个 `inaccessible` / `cannot find in scope`（SC-1）
- [ ] `audit-checklist.md` 中 A2a、A2b、A2c、A3a、A3b、B9e 六项标记为「已修复」

## Technical Details

### 两处改名的必要性（静默解析，非编译错误）

- **`BorderlessButtonStyle`**：与 SwiftUI 自带类型同名。下游写 `BorderlessButtonStyle()` **能编译通过**，但拿到的是 SwiftUI 的类型，不是 CoreDesign 的。改名为 `CoreBorderlessButtonStyle` 后不存在「解析到 SwiftUI 版本」的可能。
- **`MenuButton`**：与 macOS 上 deprecated 的 SwiftUI `MenuButton` 冲突，实测报错信息具误导性。改名为 `CoreMenuButton`。

改名须连带更新：类型声明、`ButtonStyle where Self == ...` 上的静态扩展、全部内部消费点、`#Preview`、docs 中的示例、**以及 `App/` 预览宿主**（仓库内唯一需同步适配的消费者）。

### `CheckBox` 的处置（B9e）

`CheckBox.swift:53-59` 是硬编码 `Toggle("哈哈哈哈哈")` 的演示视图，用 `@State` 而非 `@Binding`，唯一使用者是同文件 Preview。处置：**内联进 `#Preview`**，公开面只保留 `CheckBoxToggleStyle`。

注意 `CheckBox.swift` 同时是 #2 的触及点（A1 的 `:31` 改 `Color.contentPrimary`、`:21-23` 注释修正）——这正是本任务 `depends_on: [002]` 的原因，两者串行。

### 范围边界：`View.getSize` 不在补 public 之列

`Utils/View+SizeReader.swift:44,48` 的 `getSize` 同样被实测确认下游不可达，但它的处置是**删除**而非补 `public`——它随 #6 的 FR-5 死代码清理一并移除（全库唯一消费点 `BottomInputBar.swift:138` 也在 #6 删除范围内）。**本任务不要给它补 public**，否则 #6 会白删一个刚公开的 API。

### 触及文件

`CheckBox.swift`、`BorderlessButtonStyle.swift`（改名后文件名亦须改）、`ButtonRoleStyleRole.swift`、`MenuButton.swift`（同上）、`App/` 预览宿主、相关 docs。

## Dependencies

- **前置**：#2（共享 `CheckBox.swift`——#2 改 `:31` 与注释，本任务内联演示视图；阶段 1 内部串行）
- **被依赖**：#5（`CoreBorderlessButtonStyle` 改名 + `ButtonRoleStyleRole`，改同一批文件）、#6（`CheckBox.swift` 的 B9f）、#9（`CoreMenuButton` 的中文串）、#10（`ButtonRoleStyleRole` 的 D5、`MenuButton` 的 D5）、#11（`MenuButton` 的 D12）
- **冲突**：`MenuButton.swift` 是全库冲突最密的文件（#3 #5 #9 #10 #11 五方）

## Effort Estimate

- Size: M
- Hours: 8

改动本身机械（补关键字 + 改名 + 内联），但改名会横切内部消费点、Preview、docs 与 `App/` 预览宿主，须逐处扫净；且 `MenuButton.swift` 是五方冲突文件，交接需谨慎。

## Definition of Done

- [ ] `swift build` 绿、零 warning
- [ ] `swift test` 绿、零 warning
- [ ] `swift build --traits Blossom` 绿、零 warning
- [ ] `swift test --traits Blossom` 绿、零 warning
- [ ] 若本任务新增或删除了 colorset（预期不会），须先 `swift package clean` 再验证
- [ ] 下游 probe 包能构造 `CheckBoxToggleStyle`、`CoreBorderlessButtonStyle`，读取 `ButtonRoleStyleRole` 的三个调色板属性，访问第 4 层功能色
- [ ] `grep -rn "BorderlessButtonStyle\|MenuButton" Sources/ App/ docs/` 无遗留旧名（SwiftUI 自身引用除外）
- [ ] `audit-checklist.md` 中 A2a、A2b、A2c、A3a、A3b、B9e 六项的状态已更新（SC-7 判定基础）
