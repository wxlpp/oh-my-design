---
name: 可访问性
status: closed
created: 2026-07-18T22:45:26Z
updated: 2026-07-21
github: https://github.com/wxlpp/CoreDesign/issues/99
depends_on: [93, 95, 97]
parallel: true
conflicts_with: [93, 95, 97, 102]
---

# Task: 可访问性

## Description

补齐三处 VoiceOver 可访问性缺口，承载审计项 **D1a、D1b、D1c** 共 3 项（对应 FR-7 的 a11y 部分、US-5）。

三处都是同一类问题的不同形态：**图标承载的信息没有暴露给辅助技术**——icon-only 按钮无标签、选中态未播报、装饰性图标未隐藏。三处都在同仓已有正确范例，本任务是把既有做法补齐到遗漏处，不引入新模式。

本任务属于 epic 的**并行窗口 2**：owner 矩阵逐对验算确认 **008 ∩ 010 = ∅**，可与 010（公开 API 形态统一）真正并发。

## Acceptance Criteria

### D1a — `BottomInputBar` 三个 icon-only 按钮

`BottomInputBar.swift` **全文件 `accessibility` 出现 0 次**。三个 icon-only Button 补 `accessibilityLabel`：

- [ ] `:150` suggestion 按钮（`wand.and.sparkles.inverse`）
- [ ] `:160` send 按钮（`paperplane`）
- [ ] `:172` stop 按钮（`stop.fill`）

（行号为审计时基线；006 已拆分 `BottomInputBarModifier.body`，执行时按拆分后的实际位置定位这三个按钮。）

### D1b — `UnderlinedTabItem` 选中态

- [ ] `UnderlinedTabBar.swift:143` 的 `UnderlinedTabItem` 补 `.accessibilityAddTraits(isSelected ? .isSelected : [])`
- [ ] 写法与同仓既有正确范例一致：`SegmentedControl.swift:118`、`Sidebar.swift:154`

### D1c — `Form` 图标的语义区分

- [ ] `Form.swift:27` `LabelIcon` —— 纯装饰，补 `.accessibilityHidden(true)`
- [ ] `Form.swift:78` `ChevronRightIcon` —— 纯装饰，补 `.accessibilityHidden(true)`
- [ ] `Form.swift:95` `DangerIcon` —— **承载语义**，补 `accessibilityLabel`（不是隐藏）

## Technical Details

### 三处的处理原则不同，不要一律照抄

| 图标类型 | 处理 | 本任务实例 |
|---|---|---|
| icon-only 可交互控件 | `accessibilityLabel` | `BottomInputBar` 三个按钮、`Form.DangerIcon` |
| 纯装饰、信息已由邻近文本承载 | `accessibilityHidden(true)` | `Form.LabelIcon`、`Form.ChevronRightIcon` |
| 状态需播报 | `accessibilityAddTraits` | `UnderlinedTabItem` |

`DangerIcon` 是最容易误判的一处——它与另两个 `Form` 图标同文件、形态相似，但它**承载语义**（危险状态本身就是信息），必须给 label 而非隐藏。

### 字符串处理

本任务补的 accessibility 标签属新增 UI 字符串。**本轮直接写英文字面量**，String Catalog 迁移由 009（本地化）负责——009 排在本任务之后，会把这批字符串一并纳入。不要在本任务里自行引入 `.xcstrings`（那会触碰 `Package.swift`，属 009 与 001 的范围）。

### 依赖说明

- **002**：`Form.swift:101` 使用 legacy `Color.dangerForeground`，由 002 迁移到新 `StatusColors` 体系；本任务改同一文件，须在其后
- **004**：Dynamic Type 改造波及 `Form.swift` 与 `BottomInputBar.swift`（`:302,376` 的 D3 系统字号迁移），须在其后
- **006**：`BottomInputBarModifier.body` 的拆分（B8h）会移动本任务要改的三个按钮，须在其后——否则两边改同一 hunk

### 代码风格

遵循仓库既有约定：显式 `self.`、中英双语注释与 `// MARK: -`。

## Dependencies

- **前置任务**：002（`Form.swift` legacy token 迁移）、004（Dynamic Type 改造波及 `Form` / `BottomInputBar`）、006（`BottomInputBar` body 拆分）
- **可并行**：010（公开 API 形态统一），矩阵验证 008 ∩ 010 = ∅
- **冲突（须串行）**：002、004、006、011
- **下游**：009 会把本任务新增的英文字符串纳入 String Catalog；011 的 D12（`BottomInputBar.swift:219` 硬编码数值）在本任务之后

## Effort Estimate

- Size: S
- Hours: 3

## Definition of Done

- [ ] 3 个审计项（D1a、D1b、D1c）全部实施完成
- [ ] `BottomInputBar.swift` 中 `accessibility` 出现次数从 0 变为 ≥ 3
- [ ] `UnderlinedTabItem` 的选中态写法与 `SegmentedControl.swift:118` / `Sidebar.swift:154` 一致
- [ ] `Form` 的两个装饰图标 hidden、`DangerIcon` 有 label（三者处理方式不同，逐一核对）
- [ ] 四条 SwiftPM 命令全绿、零 warning：
  - [ ] `swift build`
  - [ ] `swift test`
  - [ ] `swift build --traits Blossom`
  - [ ] `swift test --traits Blossom`
- [ ] 若本轮新增或删除了 colorset，先 `swift package clean` 再跑上述四条命令
- [ ] 更新 `audit-checklist.md` 中 D1a / D1b / D1c 三项的状态（SC-7 判定基础）
- [ ] VoiceOver 冒烟验证：在 iOS Simulator 中开启 VoiceOver（或用 Accessibility Inspector）走一遍 `BottomInputBar` / `UnderlinedTabBar` / `Form`，确认三个按钮有可读标签、tab 选中态被播报、装饰图标不被聚焦
