---
name: 公开 API 形态统一
status: closed
created: 2026-07-18T22:46:02Z
updated: 2026-07-21
github: https://github.com/wxlpp/CoreDesign/issues/101
depends_on: [94, 95]
parallel: true
conflicts_with: [93, 94, 95, 96, 97, 100, 102]
---

# Task: 公开 API 形态统一

## Description

本任务承载 9 个审计项：**B8b、B8e、B8f、B8g、D4、D5、D6a、D6b、D7**。它与 004（Dynamic Type 改造）**共享 12 个文件**——是全 epic 冲突最密的一对，故必须紧随 004 之后执行（epic Implementation Strategy 阶段 4）。

与 011（机械清理）不同，本任务**每一项都是设计决策**：合并两个语义相同的枚举、把 N 个平行 switch 收敛为返回 spec 结构体的单一 switch、把布尔 hack 升级为 style 协议、把只收 `String` 的组件补 `@ViewBuilder` slot。epic 的 Estimated Effort 表把它与 006 列为同量级（约 16 文件），并注明「项数中等但每项都是设计决策」。

**可与 008（可访问性）并行**——矩阵逐对验算确认 `#10 ∩ #8 = ∅`。

## Acceptance Criteria

### 枚举与 switch 收敛

- [ ] `ToastLevel`（`Toast.swift:24-29`）与 `MessageLevel`（`Banner.swift:32-37`）合并为单一 `StatusLevel`——两者 case 集合**完全相同**，是同一概念的两次声明（B8e）
- [ ] `StateLabel` 的 4 个平行 switch（`StateLabel.swift:65-109`）收敛为**返回 spec 结构体的单一 switch**（B8f）
- [ ] `StatusRow` 的 3 个平行 switch（`StatusRow.swift:66-91`）同样收敛为 1 个（B8f）
- [ ] 两个 `BannerStyle.makeBody`（`Banner.swift:177-192,210-225`，11 行里只有最后一行不同）抽出公共实现（B8b）
- [ ] `SegmentedControl` 两处 glass 分支（`SegmentedControl.swift:121-147,379-409`）各自复制的 overlay 提取为共享构造（B8g）

### style 协议化

- [ ] `SegmentedControl` 的 `glass: Bool`（`:27,33`）升级为 `SegmentedControlStyle` 协议 + `@Entry var segmentedControlStyle` + `View.segmentedControlStyle(_:)` modifier（D7）
- [ ] 新形态与 `Banner` 的既有 style 形态对齐（协议含 `makeBody(configuration:)` + `SegmentedControlStyleConfiguration`，具体实现 + EnvironmentValues 入口），不另立平行模式

### API 表面收敛

- [ ] `public let` 存储属性降为 private：`ProgressBar.swift:22-24`、`StatusRow.swift:32-34`、`StateLabel.swift:39-40`、`CommentCard.swift:27-31`、`EventRow.swift:23-26`、`TimelineItem.swift:39-40`、`AvatarGroup.swift:23`（D4）——与已是 private 的 `Tag`/`SearchField`/`Sidebar*`/`ListRow`/`Badge` 对齐
- [ ] 语义枚举统一补 `Sendable, Equatable`：`MessageLevel`（合并后的 `StatusLevel`）、`ButtonRoleStyleRole`、`CoreMenuButtonStyle`、`SurfaceKind`（D5）
- [ ] `StateLabel` 的 init 首参加标签（`StateLabel.swift:42`），三个同类 pill 组件（`StateLabel`/`Badge`/`Tag`）init 形态统一（D6a）
- [ ] `StateLabel`、`StatusRow`、`EventRow`、`SidebarNavigationRow` 补 `@ViewBuilder` designated init，使其能插图标或富文本（D6b）
- [ ] 所有改动保持 `public`（含 init）——库的公开面不得因收敛而缩窄，参见 CLAUDE.md「公开 API 表面」

## Technical Details

### spec 结构体收敛模式（B8f）

`StateLabel` 的 4 个 switch 与 `StatusRow` 的 3 个 switch 都在对同一个枚举做多次穷举，每次取一个维度（颜色 / 图标 / 文案 / 背景）。收敛形态是引入一个私有 spec 结构体，单个 switch 返回它：

```swift
private struct Spec { let icon: String; let tint: Color; /* ... */ }
private var spec: Spec { switch self.style { /* 单次穷举 */ } }
```

收益不只是行数——新增 case 时编译器只在**一处**要求穷举，漏配某个维度的可能性归零。

### `StatusLevel` 合并的波及面（B8e）

`ToastLevel` 与 `MessageLevel` 合并后，`Toast` 与 `Banner` 两个组件的公开签名都会变。这是 breaking change，按 epic 决策**直接改，不留 deprecated 兼容层**。须同步更新：仓库内 `App/` 预览宿主、`docs/components/` 下引用这两个枚举的示例、以及测试文件中的构造点。

### `Badge` / `Tag` 是 init 形态的好范例（D6a、D6b）

`Badge.swift:83` 与 `Tag.swift:80` 已经是「String 便利 init + `@ViewBuilder` designated init」的双层形态。D6a / D6b 的目标就是让 `StateLabel`、`StatusRow`、`EventRow`、`SidebarNavigationRow` 对齐到同一形态——**便利 init 保留**（现有调用点不必全改），designated init 新增。

### `Sidebar.swift` 的额外验证要求（关键）

D6b 会重塑 `SidebarNavigationRow` 的 init 形态，而 **004 在 `Sidebar.swift` 上落了 Dynamic Type 布局断言**。该断言被 `#if os(iOS)` 包住，**四条 SwiftPM 命令下完全不可见**——在 macOS 宿主里编译都不进入。

因此本任务**必须额外跑第 5 条验证命令**：

```bash
xcodebuild test -scheme CoreDesign -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

不跑这条，弄破布局断言将无人发现（epic「每个 Issue 的完成定义」第 4 条明确点名本任务）。若 001 的 CI 降级到本地 pre-push 级别，这条命令须在本地手工执行并记录输出。

### 触及文件（约 16 个）

`Toast.swift`、`Banner.swift`、`StateLabel.swift`、`StatusRow.swift`、`SegmentedControl.swift`、`ProgressBar.swift`、`CommentCard.swift`、`EventRow.swift`、`TimelineItem.swift`、`AvatarGroup.swift`、`Sidebar.swift`、`ButtonRoleStyleRole.swift`、`CoreMenuButton`（`MenuButtonStyle` 的 `Sendable, Equatable`）、`SurfaceKind` 所在文件，加上 `App/` 预览宿主与相关测试文件。

## Dependencies

- **前置**（真实文件级）：
  - **003** —— `ButtonRoleStyleRole` 的 public 化（A2c）与 `MenuButton` → `CoreMenuButton` 改名，D5 要在这些符号上补协议
  - **004** —— **共享 12 个文件**，是全 epic 最密的冲突对。API 形态重塑应当在字体迁移后的代码上做，否则同一批 12 个文件要改两遍（epic 对 PRD 分解规则的有意修订即为此）
- **可并行**：**008**（可访问性）——矩阵验证 `#10 ∩ #8 = ∅`，008 触及 `BottomInputBar` / `UnderlinedTabBar` / `Form`，与本任务无交集
- **被依赖**：009（`CoreMenuButton`、`Toast`）、011（多个共享文件）
- **冲突**：002、003、004、005、006、009、011

## Effort Estimate

- Size: L
- Hours: 20

epic Estimated Effort 表把本任务与 006 列为同量级（约 16 文件）。但性质不同：006 是 18 项以删除为主的清理，本任务是 9 项**全为设计级改动**——`StatusLevel` 合并、style 协议化、`@ViewBuilder` init 每一项都要先定形态再落地，且都是 breaking change，须同步预览宿主、docs 与测试。工作量不在行数而在设计决策与波及面收口。

## Definition of Done

- [ ] `swift build` 绿、零 warning
- [ ] `swift test` 绿、零 warning
- [ ] `swift build --traits Blossom` 绿、零 warning
- [ ] `swift test --traits Blossom` 绿、零 warning
- [ ] **第 5 条命令绿**：`xcodebuild test -scheme CoreDesign -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`——本任务的 D6b 重塑 `Sidebar.swift` 的 row init 形态，而 004 的布局断言 `#if os(iOS)` 包住，前四条命令下不可见
- [ ] 若本任务新增或删除了 colorset（预期不会），须先 `swift package clean` 再跑上述验证
- [ ] `audit-checklist.md` 中 B8b、B8e、B8f、B8g、D4、D5、D6a、D6b、D7 九项的状态已更新（SC-7 判定基础）
- [ ] `StatusLevel` 合并的 breaking change 波及点已逐一记录：预览宿主、docs、测试文件的更新清单
- [ ] `SegmentedControlStyle` 协议形态与 `BannerStyle` 的一致性已核对（协议 + Configuration + `@Entry` + `View` 扩展四件套齐备）
