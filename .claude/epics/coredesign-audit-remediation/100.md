---
name: 本地化 String Catalog
status: closed
created: 2026-07-18T22:46:02Z
updated: 2026-07-21
github: https://github.com/wxlpp/CoreDesign/issues/100
depends_on: [92, 94, 95, 97, 101]
parallel: false
conflicts_with: [92, 93, 94, 95, 96, 97, 101, 102]
---

# Task: 本地化 String Catalog

## Description

本任务承载 1 个审计项：**D2**（硬编码中文 UI 字符串，与别处英文不一致；全库无 String Catalog）。它是全 epic 承载项最少、触及文件最少的任务（1 项 / 3 个组件文件 + `Package.swift`），但**含一处跨任务触碰**，须谨慎处理。

库内 UI 字符串目前是分裂的：一部分组件已经是英文（`ProgressBar` 的 "Progress"、`ProgressIndicator` 的 "Loading"、`Tag` 的 "Remove tag"、`AvatarGroup` 的 "more avatars"），另外四处是硬编码中文：

| 位置 | 当前字符串 | 性质 |
|---|---|---|
| `Toast.swift:441` | `"点击关闭"` | 可访问性 / 提示文案 |
| `CoreMenuButton`（原 `MenuButton.swift:139`） | `Text("菜单")` | 可见 UI 文案 |
| `CoreMenuButton`（原 `MenuButton.swift:169`） | `.accessibilityLabel("菜单")` | 可访问性标签 |
| `BookCover.swift:23` | `"未命名"` | 占位文案 |

本任务把这四处统一为英文，并把全库 UI 字符串纳入 **String Catalog（`.xcstrings`）**，使库具备可本地化能力（而非仅仅"改成英文"）。已是英文的那几处同样纳入目录，口径才算统一。

## Acceptance Criteria

- [ ] `Toast.swift:441` 的 `"点击关闭"`、`CoreMenuButton` 的 `Text("菜单")` 与 `.accessibilityLabel("菜单")`、`BookCover.swift:23` 的 `"未命名"` 全部改为英文（D2）
- [ ] `grep -rn '[\p{Han}]' Sources/CoreDesign --include=*.swift` 在 **UI 字符串字面量**范围内零命中（注释与 `// MARK:` 的中英双语按仓库既有约定保留，不在清理范围）
- [ ] `Sources/CoreDesign/Resources/` 下存在 `Localizable.xcstrings`，包含上述四条新英文串
- [ ] 已是英文的既有 UI 串（`ProgressBar` "Progress"、`ProgressIndicator` "Loading"、`Tag` "Remove tag"、`AvatarGroup` "more avatars"）同样纳入 catalog，使全库口径一致
- [ ] `Package.swift` 声明 `defaultLocalization`（SPM 硬性要求，见 Technical Details）
- [ ] 四条 SwiftPM 命令在加了 `defaultLocalization` 后仍全绿、零 warning
- [ ] `audit-checklist.md` 中 D2 标记为「已修复」

## Technical Details

### 关键前置：`Package.swift` 的 `defaultLocalization`（跨任务触碰）

**SPM 对含 `.xcstrings` 的 target 强制要求 `Package.swift` 声明 `defaultLocalization`**——不声明则包解析直接失败（`manifest property 'defaultLocalization' not set`）。

这是 owner 矩阵中一处**未建模的跨任务触碰**：`Package.swift` 归 **001**（`defaultIsolation`、`platforms` 枚举形式）。时序上无冲突（001 是阶段 0 硬前置，本任务在阶段 5），但两者改同一文件，故本任务 `depends_on` 含 001、`conflicts_with` 含 001。

约束：
- 本任务**只加 `defaultLocalization: "en"` 一行**，不碰 001 在该文件里落的 `swiftSettings` / `platforms` 改动
- 加完后须用**四条 trait 命令完整复验**（不是只跑 `swift build`）——`defaultLocalization` 影响资源打包路径，Blossom 构建同样要验

### 资源加载约定

`Localizable.xcstrings` 放在 `Sources/CoreDesign/Resources/` 下，随现有的 `.process("Resources")` 一并处理。**所有查找必须显式传 bundle**：

```swift
Text("menu.title", bundle: .module)
```

SwiftUI 默认走 main bundle，库内不传 `bundle: .module` 会在下游 app 里静默取不到译文（回落成 key）。这与 CLAUDE.md「所有资源查找都必须传入 `bundle: .module`」的既有约定一致。`.accessibilityLabel` 等接收 `LocalizedStringKey` 的 API 同样须走 `LocalizedStringResource(_:bundle:)` 或等价形态。

### `MenuButton` 已被 003 改名

**本任务须基于改名后的代码**：003 已把 `MenuButton` → `CoreMenuButton`（避开 macOS 上 SwiftUI 同名的 deprecated 类型）。审计清单里的 `MenuButton.swift:139,169` 是审计时的坐标，行号与类型名在 003、010 之后都会漂移，须按符号定位而非按行号。

### 触及文件

- `Package.swift`（加 `defaultLocalization`，与 001 共享）
- `Sources/CoreDesign/Resources/Localizable.xcstrings`（新增）
- `Components/Toast/Toast.swift`
- `Components/MenuButton/`（003 改名后的 `CoreMenuButton`）
- `Components/BookCover/BookCover.swift`
- 口径统一涉及：`ProgressBar`、`ProgressIndicator`、`Tag`、`AvatarGroup`

## Dependencies

- **前置**（真实文件级）：
  - **001** —— `Package.swift`：本任务要加 `defaultLocalization`，须在 001 的 `swiftSettings` / `platforms` 改动之后
  - **003** —— `MenuButton` → `CoreMenuButton` 改名，本任务须基于改名后的代码
  - **004** —— Dynamic Type 改造触及 `Toast` / `BookCover`（`CoreTypography` 消费者），字体迁移后再改字符串
  - **006** —— `BookCover` 的图片解码移出 body（B9a）改同一文件
  - **010** —— `CoreMenuButton` 与 `Toast` 的 API 形态统一（`StatusLevel` 合并触及 `Toast`），须在其后
- **可并行**：008（可访问性）——两者交集为空
- **冲突**：与 001、002、003、004、005、006、010、011 均有共享文件，须串行

## Effort Estimate

- Size: XS
- Hours: 3

全 epic 最轻的任务（epic Estimated Effort 明确点名「最轻的是 #9（1 项 / 3 文件）」）。改动本身是四个字符串 + 一个 catalog 文件；工作量的不确定性只在 `defaultLocalization` 首次引入后的四条命令复验，以及确认 `bundle: .module` 在各调用形态（`Text` / `accessibilityLabel` / `LocalizedStringResource`）下都正确传递。

## Definition of Done

- [ ] `swift build` 绿、零 warning
- [ ] `swift test` 绿、零 warning
- [ ] `swift build --traits Blossom` 绿、零 warning
- [ ] `swift test --traits Blossom` 绿、零 warning
- [ ] 若本任务新增或删除了 colorset（预期不会），须先 `swift package clean` 再跑上述验证
- [ ] `audit-checklist.md` 中 D2 的状态已更新（SC-7 判定基础）
- [ ] `Package.swift` 的 `defaultLocalization` 改动已记录，并确认未影响 001 落的 `swiftSettings` / `platforms`
- [ ] 全库 UI 字符串字面量的中文清零已用 grep 核实并记录输出
