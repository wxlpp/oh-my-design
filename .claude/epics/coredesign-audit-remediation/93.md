---
name: 色彩层重组
status: closed
created: 2026-07-18T22:44:39Z
updated: 2026-07-19
github: https://github.com/wxlpp/CoreDesign/issues/93
depends_on: [92]
parallel: false
conflicts_with: [94, 95, 97, 99, 100, 101, 102]
---

# Task: 色彩层重组

## Description

承载 12 个审计项：A1、A2d、B1a、B1b、B1c、B6a、B6b、B6c、D11、D13、D14、D19（对应 FR-1）。

核心动作是**把第 4 层色彩的职责收窄为「状态功能别名」**（epic 的 AD-1）：删除 `FunctionalColor` 的 `primary`/`secondary`/`tertiary` 三组，交互色统一走第 3 层 `InteractionColors`。这一个动作同时解决四件事——A1 遮蔽 bug、库内外语义分裂、与 `InteractionColors` 逐值重复的色阶、以及一处零效果的 Blossom 分流。

**本任务最大的技术风险是 A1 型静默重解析**：`Color.primary/secondary/tertiary` 遮蔽 SwiftUI 同名成员，删除这些符号时**编译器不报错**，只会静默把解析目标改到 SwiftUI 内建成员。因此必须用下述**毒丸机制**让编译器穷举残留使用点，而不是靠「逐处显式改写」这种过程纪律。

## Acceptance Criteria

- [ ] `FunctionalColor` 的 `primary`/`secondary`/`tertiary` 三组共 12 个符号（3 基础名 + 9 个 Active/Disable/Hover 变体）已删除（B1a、B1b、B1c）
- [ ] 毒丸 commit 已执行并留在历史中，其编译诊断输出（全部残留使用点清单）已记录
- [ ] `FunctionalColor` extension 整层补 `public`；`success`/`info`/`warning`/`danger` 及现有变体按现状保留（A2d）
- [ ] `CheckBox.swift:31` 改用 `Color.contentPrimary`，21–23 行的矛盾注释已修正（A1）
- [ ] `StatusRow.swift:80` 的 `case .skipped` 显式改写为期望的中性语义 token（A1，NFR 视觉例外第 10 条）
- [ ] `CoreGradient+Preview.swift:17` 显式改为期望的语义 token（B1c）
- [ ] `danger` 基准 `red4` → `red5`（D11，NFR 视觉例外第 1 条）
- [ ] `borderSelected` → `.accent`；`borderFocus` → `.accent`，**不新增 `#if Blossom`**（D14、AD-2）
- [ ] `selectionBackgroundEmphasis` 走命名别名，`BorderColors.swift:53` 与 `InteractionColors.swift:32` 的层级违规消除（D13）
- [ ] `BorderColors.swift:50` 与代码矛盾的注释已修正（D14）
- [ ] **`statusAccent*` 保留**（原判「整组删除」已于执行期改判——见下方 Technical Details）；改为修正**五组** `status-*-emphasis` 的 light 值（D19）
- [ ] `StatusColors` 新体系补 `*Border` 档（B6c）；`Toast`/`Badge`/`Banner`/`Form` 四处全量迁移；legacy 组 `StatusColors.swift:63-77` 删除（B6a、B6b）
- [ ] `StatusColorsTests.swift` 重新编译通过（否则本任务自身验证不绿）
- [ ] `grep -rn "#if Blossom" Sources/ | wc -l` 从 9 降至 8（SC-2）
- [ ] `audit-checklist.md` 中 12 项标记为「已修复」

## Technical Details

### A1 毒丸机制（必做，不可替代）

三处被遮蔽符号的引用（`CheckBox.swift:31`、`StatusRow.swift:80`、`CoreGradient+Preview.swift:17`）在符号删除后**不报错**，只会静默改解析到 SwiftUI 内建成员。

**做法**：先不删符号，而是加一个中间 commit，给 `FunctionalColor` 的 12 个遮蔽符号全部标注：

```swift
@available(*, deprecated, message: "A1 probe: shadows SwiftUI builtin; use contentPrimary or an explicit token")
```

编译一次，编译器会**精确报出全部残留使用点**（含审计未发现的）。逐处改写至零诊断后，再删符号。

**必须用 `deprecated`，严禁用 `unavailable`。** epic 已实测四种方案：

| 方案 | 结果 |
|---|---|
| `@available(*, unavailable)` | **零诊断**，静默通过。重载决议把 unavailable 候选**排除**出候选集，直接落到 SwiftUI 内建成员 |
| 改名（`primary` → `__a1_primary`） | 零诊断，同样静默落到 SwiftUI |
| 移出 `Color` 扩展到独立命名空间 | 零诊断，同上 |
| **`@available(*, deprecated)`** | **精确报出每个使用点**：`warning: 'primary' is deprecated: A1 probe` |

差别在于 `deprecated` 候选**仍参与重载决议并胜出**，只是附带诊断；`unavailable` 候选被踢出决议。越"强"的标记对本场景越无效。

**硬闸门**：配合 `-warnings-as-errors` 可让残留点无法被忽略（实测输出 `error:` 而非 `warning:`）：

```bash
swiftc -typecheck -warnings-as-errors ...   # 或临时给 target 加 .unsafeFlags(["-warnings-as-errors"])
```

### `statusAccent*` 保留（原判「整组删除」已改判）

**改判理由**：删除它与本任务自身的 B6a（`Banner`/`Toast`/`Badge` 迁出 legacy）**直接冲突**。新体系只有 `accent` 一个蓝色家族，而它正是 Primer 的 info 语义——那三个组件的 legacy `info*` 只能迁到它。D19 原判据「库内零渲染消费点」恰恰是因为 info 当时走 legacy，迁移一做该判据即失效。

**D19 同时改判**：原记「`statusAccentEmphasis` 的 light 值是 accent 单组的 colorset 笔误」。横向比对五组后发现 `accent`/`success`/`attention`/`danger`/`done` 的 `*-emphasis` light 值**逐组等于同组 `*-muted`**（`#DDF4FF`/`#AFF5B5`/`#F8E3A1`/`#FFC1BA`/`#DACDFB`），是整个新体系的系统性错误。Primer 语义里 emphasis 是饱和实色。

**改为**：修正五组 emphasis 的 light 值为各组 `fg` 同色（accent `#0969DA`、success `#1F883D`、attention `#9A6700`、danger `#CF222E`、done `#8250DF`），四个 `status-accent-*.colorset` **保留不删**。

### legacy status 组迁移（四处，漏一处即编译失败）

`Toast` / `Badge` / `Banner` / **`Form`** 全量迁移到新体系。**`Form.swift:101` 使用 legacy `Color.dangerForeground`，`:92,99` 的文档注释同样引用——漏改会直接编译失败**，这是最容易被忽略的一处（`Form` 不在直觉上的「status 组件」列表里）。

`Badge` 仍留在 legacy 的直接原因是新体系缺 `*Border` 档（B6c），故补档是迁移的前置。

### `borderFocus` → `.accent` 的视觉代价

通过**别名继承**实现 Blossom 跟随，与 `borderSelected` 的既有处理一致，使全库分流点净减（9 → 8）。

代价：**默认主题下 focus ring 从 Primer 蓝 `#0969DA` 变为品牌蓝 `#0077FA`**（dark 从 `#1F6FEB` 变 `#3295FB`），影响两个真实渲染点：`FocusRingModifier.swift:106`（默认参数）与 `SearchField.swift:136`。这是已在 NFR 中声明的视觉例外第 3 条，不是回归。

### `StatusColorsTests` 必须重新编译通过

`StatusColorsTests.swift` 的 `:48-62` 引用 12 个 legacy token，该组被本任务删除，故该文件编译失败。（`:10-13` 引用的四个 `statusAccent*` **保留**，无需处理——见上方改判。）

**本任务负责让它编译通过**（删掉引用已删 token 的断言）——否则 `swift test` 必红，本任务自身验证不绿。**恒真断言的整体清理归 #7**，本任务不做，只保证编译通过。

### 仓内触及清单（删除 token 会波及但不一定报错的地方）

- `Tests/CoreDesignTests/StatusColorsTests.swift` —— 见上
- `App/Sources/Previews.swift:233` —— `Circle().fill(Color.statusAccentEmphasis)`。因 accent 保留，**代码无需改动**；但该 token 的 light 值会从淡蓝洗色变为饱和 Primer 蓝（emphasis 修正的结果），确认这是期望结果即可
- **4 个 docs 文件**：`docs/components/timeline-item.md:24`（`statusAccentEmphasis`）、`form-icons.md:27,67`、`banner.md:34`、`toast.md:65`（legacy token）。`docs/superpowers/plans/` 下是归档，**不改**
- **源码内注释**（漏改只是文档漂移不报错，须逐一处理）：`Toast.swift:18`、`Banner.swift:26,68,167,201`、`Badge.swift:55`、`Form.swift:92,99`
- CLAUDE.md 的分层描述须同步更新

### 触及规模

11 个 swift 文件 + 4 个 colorset 新建 + 1 个删除 + 5 个改值 + 4 个 docs + CLAUDE.md。

## Dependencies

- **前置**：#1（`defaultIsolation` 改变全库编译语义，须最先合入）
- **被依赖**：#3（共享 `CheckBox.swift`）、#4、#6、#7（`StatusColorsTests` 由本任务负责编译通过）、#8（`Form.swift`）、#11（`Banner.swift`）
- **冲突**：与 #3、#4、#6、#8、#9、#10、#11 有共享文件（`CheckBox` / `Toast` / `Badge` / `Banner` / `Form` / `StatusRow` / CLAUDE.md）

## Effort Estimate

- Size: L
- Hours: 16

12 个承载项中含**三处 A1 型静默重解析**，这是全 epic 唯一需要毒丸 commit 兜底的地方；另有 4 个 border colorset **新建**、1 个 `border-focus.colorset` 删除、5 个 emphasis 改值、4 处 legacy 迁移、4 个 docs 同步。项数与波及面双高。

## Definition of Done

- [ ] `swift build` 绿、零 warning
- [ ] `swift test` 绿、零 warning
- [ ] `swift build --traits Blossom` 绿、零 warning
- [ ] `swift test --traits Blossom` 绿、零 warning
- [ ] **新增 4 个 border colorset（及删除 `border-focus.colorset`）后已执行 `swift package clean`，再跑上述四条命令**（不 clean 时增量构建不拷贝新目录，验证假绿）
- [ ] 毒丸 commit 阶段的编译诊断输出已归档；改写至零诊断后才删符号
- [ ] `grep -rn "#if Blossom" Sources/ | wc -l` 输出为 8
- [ ] `StatusColorsTests.swift` 编译通过（恒真断言清理留给 #7）
- [ ] `audit-checklist.md` 中 A1、A2d、B1a、B1b、B1c、B6a、B6b、B6c、D11、D13、D14、D19 共 12 项的状态已更新（SC-7 判定基础）
- [ ] 本任务执行期的**三条**改判（`statusAccent*` 保留 / 五组 emphasis 系统性错误 / 迁移改变 light+dark 观感）已同步进 `93.md`（本文件）、`audit-checklist.md`、PRD 的 US-3 / SC-2 / FR-1、以及 **`epic.md` 的 AD-2 本体**
