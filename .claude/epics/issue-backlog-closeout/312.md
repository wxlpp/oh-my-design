---
name: 4 组件形态 D 扩展点 + OrbitingLogos 落点裁定
status: open
created: 2026-09-14T00:33:25Z
updated: 2026-09-14T00:33:25Z
github: https://github.com/wxlpp/oh-my-design/issues/312
depends_on: []
parallel: false
conflicts_with: [337]
---

# 任务：#312 组件样式扩展点（J-2 红名单承接）

规格见 issue 正文 + 两条评论。`NetworkGraph` 已由 `#355` 以形态 D2 落地（红名单 5 → 4），
剩 4 条：`RadarChart` / `RingChart` / `ActivityHeatmap` / `BeforeAfterSlider`。

## 关键约束（来自 issue + 评论，逐条有效）

1. **禁形态 B public 协议**（`D-299-1` 修订回路没走完，不可逆风险）⇒ 全部走形态 D
   （槽 / 枚举可演进；判据已承认、登记表已有 6 个先例）。
2. **`OrbitingLogos` 裁定**：来源 2026-09-14 实测 HTTP 200、正文含 `OrbitingItem`
   `radiusX` / `radiusY` / `tiltAngle` props（已留档，随 PR 进 `docs/issues/`）⇒
   按 `D-299-2` 字面计入 2 ≥ 2 ⇒ **出口 1 ⇒ `semantic` ⇒ 需要扩展点**；
   最终态：J-2 定义域 17 条、红名单空集并删除（翻转与五条扩展点同一提交落地，无中间态）；
   裁定留痕在 `docs/contract-defects.md` 的 `## #312` 节 + `R-49`。
3. **全口径重核 `D-299-1`**：`RadarChart` / `ActivityHeatmap` 的「不命中」须按
   「宿主平台框架的具名 API」重核，起点 = issue 里两个具名反例
   （`UICalendarView` + `UICalendarViewDecoration`；`SectorMark(angle:innerRadius:outerRadius:)`）。
   重核结果写进 `docs/contract-defects.md` 的 `D-299-1`《`#312` 重核》段与 `## #312` 节，不改落点。
4. **形态 D 成立条件**：槽 / 枚举须**完整承载**判定时枚举的候选形态差异
   （各条候选清单见 issue 正文；「填了不覆盖比不填更糟」）。

## 完成判据（issue 原文）

1. 5 条（含翻至出口 1 的 `OrbitingLogos`）各自的 `styleSlot` / `styleEnum` / `customStyleProtocol` / `nativeProtocol`
   之一在登记表填上且源码真实存在；
2. `knownMissingExtensionPoints` 收缩为空集，`withKnownIssue` 块按到期机制删除，
   `extensionPointFollowUpIssue` + 聚合断言一并删除；
3. `docs/components/*.md` 与登记表 `notes` 同步；
4. `OrbitingLogos` 落点裁定留痕（台账 + `docs/contract-defects.md`）。

## 已知坑

- `#337` 与本任务同触 `contract-defects.md` / `component-registry.json` ⇒ 串行，等 `#337` 合入后开工。
- 扩展点进 `ComponentRegistryGuard` 的判定表后，`inspected.count`（最终 17）、
  `withKnownIssue` 块（已删）、`R-48` 更正表（追加行指向 `R-49`）都要同步——照 CLAUDE.md
  的更正传播约定 grep 三处落点。
- 台账键对新 target 带 `<Target>/` 前缀；`RadarChart` 等在 `OhMyDesignCharts` target。
