---
name: docs/README 索引导航 + 快捷脚本
status: closed
created: 2026-05-10T12:20:00Z
updated: 2026-05-10
github: https://github.com/wxlpp/CoreDesign/issues/57
depends_on: ["55"]
parallel: true
conflicts_with: []
---

# Task: docs/README 索引导航 + 快捷脚本

## Description

创建 `docs/README.md` 作为组件文档索引页，含分类缩略图网格和快速导航。提供一键运行 App 和生成快照的脚本。

## Acceptance Criteria
- [ ] `docs/README.md` 存在，列出所有组件并附缩略图
- [ ] 组件按分类（Button / Form / Indicator / Layout / Navigation / Feedback）分组
- [ ] 每个组件有缩略图（light 模式 `snapshots/<name>_light.png`）和链接到对应 `.md`
- [ ] 提供 `scripts/run-preview.sh` 一键启动 Simulator 运行 App
- [ ] 提供 `scripts/run-snapshots.sh` 一键生成所有快照
- [ ] Repository root `README.md`（如已存在则更新）加入指向 `docs/README.md` 的链接

## Technical Details

### docs/README.md 结构

```markdown
# CoreDesign 组件库

iOS 26+ SwiftUI 设计系统，含 16 个 Primer 对齐组件。

## 组件索引 / Component Index

### Button 按钮
| 组件 | 预览 | 文档 |
|---|---|---|
| Button | ![](snapshots/button_light.png) | [docs](components/button.md) |
| MenuButton | ![](snapshots/menubutton_light.png) | [docs](components/menubutton.md) |

### Form 表单
| ... |

### Indicator 指示器
| Badge | Tag | Banner | Toast |
...
```

### scripts/run-preview.sh

```bash
#!/bin/bash
set -euo pipefail
xcodebuild build \
  -project App/CoreDesignPreview.xcodeproj \
  -scheme CoreDesignPreview \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -quiet
xcrun simctl boot "iPhone 16" 2>/dev/null || true
open -a Simulator
xcrun simctl install booted App/.build/...
echo "App installed. Tap CoreDesignPreview icon in Simulator."
```

### scripts/run-snapshots.sh

```bash
#!/bin/bash
set -euo pipefail
mkdir -p docs/snapshots
TEST_RUNNER_SNAPSHOTS_EXPORT_DIR=docs/snapshots \
xcodebuild test \
  -project App/CoreDesignPreview.xcodeproj \
  -scheme SnapshotTests \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -quiet
echo "Snapshots saved to docs/snapshots/"
```

## Dependencies

- Task 3: 快照已生成（需要缩略图）

## Effort Estimate
- Size: XS
- Hours: 0.5

## Definition of Done
- [ ] `docs/README.md` 存在且内容完整
- [ ] 两个脚本可执行并通过 `shellcheck`
- [ ] 截图缩略图在 GitHub 上可正常渲染
