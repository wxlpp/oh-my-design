---
name: 创建 App project + SPM 配置
status: closed
created: 2026-05-10T12:20:00Z
updated: 2026-05-10
github: https://github.com/wxlpp/CoreDesign/issues/53
depends_on: []
parallel: false
conflicts_with: []
---

# Task: 创建 App project + SPM 配置

## Description

在仓库根目录下创建 `App/CoreDesignPreview.xcodeproj`，配置 iOS App target（SwiftUI, iOS 26+），以 local SPM package 方式引用 CoreDesign 库和 SnapshotPreviews。

## Acceptance Criteria
- [ ] `App/` 目录下存在可直接 `xcodebuild` 构建的 Xcode project
- [ ] App target 编译通过（`xcodebuild -project App/CoreDesignPreview.xcodeproj -scheme CoreDesignPreview -destination 'platform=iOS Simulator,name=iPhone 16' build`）
- [ ] App target 依赖 CoreDesign（本地路径 `../`）和 SnapshotPreviews（GitHub SPM）
- [ ] 部署目标 iOS 26.0，Swift 6 语言模式
- [ ] 包含 `#Preview` block 的 app entry point 可正常在 Xcode canvas 中预览

## Technical Details

```
App/
├── CoreDesignPreview.xcodeproj/
│   └── project.pbxproj
├── Sources/
│   └── App.swift              # @main struct CoreDesignPreviewApp: App { WindowGroup { ContentView() } }
└── (Info.plist — Xcode 自动生成)
```

- App target: `CoreDesignPreview`，product type `com.apple.product-type.application`
- SPM deps：CoreDesign (file://../) + SnapshotPreviews (https://github.com/EmergeTools/SnapshotPreviews)
- 无需 Info.plist 手写，Xcode 16+ 自动生成
- `ContentView` 初始为 placeholder（Task 2 实现）

## Dependencies

无

## Effort Estimate
- Size: S
- Hours: 1

## Definition of Done
- [ ] Xcode project 创建并 push 到仓库
- [ ] `xcodebuild build` 成功
- [ ] `#Preview` 在 Xcode canvas 可见 "Hello, CoreDesign!"
